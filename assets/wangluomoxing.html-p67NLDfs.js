import{_ as e,c as l,e as s,o as n}from"./app-Cm4IjFjh.js";const d={};function o(i,t){return n(),l("div",null,[...t[0]||(t[0]=[s(`<h2 id="用户空间与内核空间" tabindex="-1"><a class="header-anchor" href="#用户空间与内核空间"><span>用户空间与内核空间</span></a></h2><p>在操作系统中，为了防止用户进程冲突导致系统崩溃，将用户与内核的空间分开：</p><ul><li>用户进程只能够调用用户空间，但可以通过系统提供的系统调用来调用内核资源</li><li>内核空间可以执行特权指令调用系统资源</li></ul><p>比如linux在写时，会将用户缓存数据拷贝到内核缓冲区，然后写入设备</p><p>读时，从设备获取数据到内核缓冲区，然后拷贝到用户缓冲区</p><h2 id="阻塞io-bio" tabindex="-1"><a class="header-anchor" href="#阻塞io-bio"><span>阻塞IO（BIO）</span></a></h2><p>即第一阶段必须阻塞进程执行下一步操作：</p><p>以linux的读写为例，用户进程执行系统调用后，必须阻塞等待结果：等待数据写入内核缓冲区，等待数据拷贝至用户缓冲区。</p><h2 id="非阻塞io-nio" tabindex="-1"><a class="header-anchor" href="#非阻塞io-nio"><span>非阻塞IO（NIO）</span></a></h2><p>即第一阶段必定不阻塞进程执行下一步操作（直接返回结果）：</p><p>用户进程尝试获取数据，若内核缓冲区没有数据，则直接返回失败，只有数据已存在内核缓冲区才会直接返回结果</p><h2 id="io多路复用" tabindex="-1"><a class="header-anchor" href="#io多路复用"><span>IO多路复用</span></a></h2><blockquote><p>减少IO带来的时间开销，尽可能的使Redis的单线程高效，全时间段的执行业务 注意是阻塞等待的，并非NIO</p></blockquote><p>在单线程下，我们对于请求的处理只能是以队列的方式一个一个来，这样效率非常低下，尤其是<strong>轮到的socket连接尚未完成IO操作</strong>，如果不采用抢占式的处理，就会导致单线程被阻塞，其他请求必须等待。</p><p>针对这样的问题，我们可以<strong>准备多IO通道同时监听请求是否已经完成IO等以确保就绪</strong>并优先处理这些请求来避免一个连接阻塞全部连接，那么我们要怎么实现对socket的监听呢？</p><p>在linux中，socket会被记录为一个文件，我们就可以通过监听他的**文件描述符（FD）**来完成就绪状态的监听，准备一个单线程专门用于监听多个FD，当某个FD表示为已就绪时就可以得到通知。</p><p>而这个单线程监听通知，其实也有多种方案：</p><h3 id="select" tabindex="-1"><a class="header-anchor" href="#select"><span>select</span></a></h3><p><strong>🔹 核心机制：</strong></p><ul><li>使用 <strong>位图（bitmask）</strong> 表示 fd 集合（<code>fd_set</code>，通常 1024 位 → 最多监听 1024 个 fd）</li><li>每次调用需 <strong>传入完整 fd 集合</strong></li><li>内核<strong>遍历</strong>所有 fd，检查是否就绪</li><li>返回时，<strong>修改原 fd_set</strong>，仅保留就绪的 fd</li></ul><p><strong>⚠️ 缺点：</strong></p><table><thead><tr><th style="text-align:left;">问题</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>fd 数量限制</strong></td><td style="text-align:left;">默认 <code>FD_SETSIZE = 1024</code>，不可动态扩展</td></tr><tr><td style="text-align:left;"><strong>每次拷贝开销大</strong></td><td style="text-align:left;">用户态 ↔ 内核态 <strong>全量拷贝 fd_set</strong></td></tr><tr><td style="text-align:left;"><strong>线性扫描 O(n)</strong></td><td style="text-align:left;">内核需遍历所有 fd（即使只有 1 个活跃）</td></tr><tr><td style="text-align:left;"><strong>不可重用</strong></td><td style="text-align:left;">每次调用必须重新设置 fd_set</td></tr></tbody></table><p><strong>✅ 优点：</strong></p><ul><li>跨平台（Windows / Unix / Linux 都支持）</li><li>简单易用（早期网络程序常用）</li></ul><h3 id="poll" tabindex="-1"><a class="header-anchor" href="#poll"><span>poll</span></a></h3><p><strong>🔹 改进点：</strong></p><ul><li>使用 <strong>数组 of <code>pollfd</code></strong> 替代位图 → <strong>突破 1024 限制</strong></li><li>分离“关注事件”和“返回事件” → <strong>无需重置结构</strong></li></ul><p><strong>⚠️ 仍存在的问题：</strong></p><table><thead><tr><th style="text-align:left;">问题</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>全量拷贝</strong></td><td style="text-align:left;">每次调用仍需将整个 <code>pollfd</code> 数组从用户态拷贝到内核态</td></tr><tr><td style="text-align:left;"><strong>O(n) 扫描</strong></td><td style="text-align:left;">内核仍需线性遍历所有 fd</td></tr><tr><td style="text-align:left;"><strong>无就绪缓存</strong></td><td style="text-align:left;">每次调用都要重新检查所有 fd</td></tr></tbody></table><p><strong>✅ 优点：</strong></p><ul><li>无 fd 数量硬限制（受限于内存）</li><li>接口更清晰（事件分离）</li></ul><h3 id="epoll" tabindex="-1"><a class="header-anchor" href="#epoll"><span>epoll</span></a></h3><table><thead><tr><th style="text-align:left;">特性</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>1. 内核事件表</strong></td><td style="text-align:left;">通过 <code>epoll_ctl</code> <strong>预先注册 fd</strong>，内核维护<strong>红黑树 + 就绪链表</strong></td></tr><tr><td style="text-align:left;"><strong>2. 零拷贝通知</strong></td><td style="text-align:left;"><code>epoll_wait</code> <strong>只返回就绪的 fd</strong>，无需全量传递</td></tr><tr><td style="text-align:left;"><strong>3. O(1) 事件检测</strong></td><td style="text-align:left;">基于回调机制：socket 就绪时，<strong>主动加入就绪链表</strong></td></tr></tbody></table><p><strong>📌 工作流程：</strong></p><ol><li><code>epoll_create()</code> → 创建内核事件表</li><li><code>epoll_ctl(ADD, fd, event)</code> → 将 fd 加入红黑树，并注册回调</li><li>当 fd 就绪 → 内核自动将其加入 <strong>就绪队列</strong></li><li><code>epoll_wait()</code> → 直接返回就绪队列中的 fd（<strong>无需遍历全部</strong>）</li></ol><p><strong>✅ 优势总结：</strong></p><table><thead><tr><th style="text-align:left;">对比项</th><th style="text-align:left;"><code>select</code>/<code>poll</code></th><th style="text-align:left;"><code>epoll</code></th></tr></thead><tbody><tr><td style="text-align:left;"><strong>fd 数量</strong></td><td style="text-align:left;">有限 / OOM 风险</td><td style="text-align:left;">百万级（受限于内存）</td></tr><tr><td style="text-align:left;"><strong>时间复杂度</strong></td><td style="text-align:left;">O(n) 每次调用</td><td style="text-align:left;">O(1) 事件通知</td></tr><tr><td style="text-align:left;"><strong>数据拷贝</strong></td><td style="text-align:left;">每次全量拷贝</td><td style="text-align:left;">仅拷贝就绪 fd</td></tr><tr><td style="text-align:left;"><strong>适用场景</strong></td><td style="text-align:left;">小规模连接</td><td style="text-align:left;"><strong>高并发（C10K+）</strong></td></tr></tbody></table><blockquote><p>💡 <strong>Redis、Nginx、Kafka 等均使用 <code>epoll</code> 实现网络模型</strong>。</p></blockquote><table><thead><tr><th style="text-align:left;">特性</th><th style="text-align:left;"><code>select</code></th><th style="text-align:left;"><code>poll</code></th><th style="text-align:left;"><code>epoll</code></th></tr></thead><tbody><tr><td style="text-align:left;"><strong>跨平台</strong></td><td style="text-align:left;">✅ 是</td><td style="text-align:left;">✅ 是</td><td style="text-align:left;">❌ Linux 专属</td></tr><tr><td style="text-align:left;"><strong>最大 fd 数</strong></td><td style="text-align:left;">1024（默认）</td><td style="text-align:left;">无硬限制</td><td style="text-align:left;">无硬限制（<code>ulimit -n</code>）</td></tr><tr><td style="text-align:left;"><strong>fd 存储结构</strong></td><td style="text-align:left;">位图（<code>fd_set</code>）</td><td style="text-align:left;">数组（<code>pollfd</code>）</td><td style="text-align:left;">内核红黑树 + 就绪链表</td></tr><tr><td style="text-align:left;"><strong>每次调用开销</strong></td><td style="text-align:left;">全量拷贝 + O(n) 扫描</td><td style="text-align:left;">全量拷贝 + O(n) 扫描</td><td style="text-align:left;"><strong>仅返回就绪 fd</strong></td></tr><tr><td style="text-align:left;"><strong>事件通知效率</strong></td><td style="text-align:left;">低（活跃 fd 少时浪费大）</td><td style="text-align:left;">中</td><td style="text-align:left;"><strong>极高（O(1)）</strong></td></tr><tr><td style="text-align:left;"><strong>支持 ET 模式</strong></td><td style="text-align:left;">❌ 否</td><td style="text-align:left;">❌ 否</td><td style="text-align:left;">✅ 是（<code>EPOLLET</code>）</td></tr><tr><td style="text-align:left;"><strong>典型应用</strong></td><td style="text-align:left;">老旧系统、教学</td><td style="text-align:left;">中等规模服务</td><td style="text-align:left;"><strong>Redis / Nginx / 高并发服务</strong></td></tr></tbody></table><h2 id="io多路复用的事件通知机制-lt-et" tabindex="-1"><a class="header-anchor" href="#io多路复用的事件通知机制-lt-et"><span>IO多路复用的事件通知机制（LT，ET）</span></a></h2><p><strong>LT（Level-Triggered，水平触发）</strong> 与 <strong>ET（Edge-Triggered，边缘触发）</strong> 是 I/O 多路复用中两种<strong>事件通知的语义模型</strong>，它们决定了操作系统在什么条件下向应用程序“报告”一个文件描述符（如 socket）已就绪。虽然 <code>select</code> 和 <code>poll</code> 只支持 LT，但 <strong><code>epoll</code> 同时支持 LT 和 ET</strong>，这也是 <code>epoll</code> 更强大、更灵活的关键原因之一。</p><table><thead><tr><th style="text-align:left;">模型</th><th style="text-align:left;">触发条件</th><th style="text-align:left;">类比</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>LT（水平触发）</strong></td><td style="text-align:left;"><strong>只要 fd 处于“就绪状态”，就持续通知</strong></td><td style="text-align:left;">就像水位传感器：只要水位高于警戒线，就一直报警</td></tr><tr><td style="text-align:left;"><strong>ET（边缘触发）</strong></td><td style="text-align:left;"><strong>仅当 fd 状态“从非就绪变为就绪”时，通知一次</strong></td><td style="text-align:left;">就像门铃：只在你按下的瞬间响一次，之后不再响</td></tr></tbody></table><blockquote><p>🔑 <strong>关键区别：是否“重复通知”未处理完的就绪事件。</strong></p></blockquote><p><strong>触发条件：</strong></p><table><thead><tr><th style="text-align:left;">事件类型</th><th style="text-align:left;">LT 触发条件</th><th style="text-align:left;">ET 触发条件</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>可读（EPOLLIN）</strong></td><td style="text-align:left;">接收缓冲区 <strong>有数据</strong></td><td style="text-align:left;">接收缓冲区 <strong>从空 → 非空</strong></td></tr><tr><td style="text-align:left;"><strong>可写（EPOLLOUT）</strong></td><td style="text-align:left;">发送缓冲区 <strong>有空闲空间</strong></td><td style="text-align:left;">发送缓冲区 <strong>从满 → 有空闲</strong></td></tr><tr><td style="text-align:left;"><strong>对端关闭（EPOLLHUP）</strong></td><td style="text-align:left;">对端关闭连接</td><td style="text-align:left;">对端关闭连接（通常也只触发一次）</td></tr></tbody></table><blockquote><p>💡 注意：<strong>ET 模式下，如果没处理完事件，可能永远丢失数据！</strong></p></blockquote><p><strong>对比：</strong></p><table><thead><tr><th style="text-align:left;">维度</th><th style="text-align:left;">LT</th><th style="text-align:left;">ET</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>系统调用次数</strong></td><td style="text-align:left;">多（重复通知）</td><td style="text-align:left;">少（仅状态变化时通知）</td></tr><tr><td style="text-align:left;"><strong>CPU 开销</strong></td><td style="text-align:left;">略高（尤其活跃连接少时）</td><td style="text-align:left;">更低（适合高并发）</td></tr><tr><td style="text-align:left;"><strong>编程复杂度</strong></td><td style="text-align:left;">简单</td><td style="text-align:left;">复杂（需处理不完整 I/O）</td></tr><tr><td style="text-align:left;"><strong>容错性</strong></td><td style="text-align:left;">高（不怕漏读）</td><td style="text-align:left;">低（漏读 = 数据丢失）</td></tr><tr><td style="text-align:left;"><strong>适用场景</strong></td><td style="text-align:left;">通用服务（如 Redis 默认）</td><td style="text-align:left;">极致性能场景（如 DPDK、自研框架）</td></tr></tbody></table><blockquote><p>LT模式下，epoll_wait读取完成后，若仍有fd残留没有读完，那么就不会将任意一个连接fd从就绪队列中删除，而ET则会直接删除</p><table><thead><tr><th style="text-align:left;">行为</th><th style="text-align:left;">LT 模式</th><th style="text-align:left;">ET 模式</th></tr></thead><tbody><tr><td style="text-align:left;">收到 10 字节</td><td style="text-align:left;">标记为就绪</td><td style="text-align:left;"><strong>触发一次通知</strong>（状态变化）</td></tr><tr><td style="text-align:left;">读 5 字节</td><td style="text-align:left;">仍就绪 → <strong>下次还通知</strong></td><td style="text-align:left;">仍就绪 → <strong>下次不通知！</strong></td></tr><tr><td style="text-align:left;">读完 10 字节</td><td style="text-align:left;">退出就绪</td><td style="text-align:left;">退出就绪</td></tr><tr><td style="text-align:left;">新数据到来（3 字节）</td><td style="text-align:left;">再次就绪 → 通知</td><td style="text-align:left;"><strong>状态变化 → 再次通知</strong></td></tr></tbody></table><p>但是<strong>旧数据一旦被读走（从 socket 接收缓冲区中取出），就永远消失了，不可能再被读到。</strong></p><table><thead><tr><th style="text-align:left;">阶段</th><th style="text-align:left;">LT 行为</th><th style="text-align:left;">ET 行为</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>1. 收到 &quot;HelloWorld&quot;</strong></td><td style="text-align:left;">通知可读</td><td style="text-align:left;">通知可读（状态变化）</td></tr><tr><td style="text-align:left;"><strong>2. 你读完 &quot;HelloWorld&quot;</strong></td><td style="text-align:left;">不再通知（缓冲区空）</td><td style="text-align:left;">不再通知</td></tr><tr><td style="text-align:left;"><strong>3. 收到 &quot;NewMsg&quot;</strong></td><td style="text-align:left;"><strong>再次通知</strong>（缓冲区非空）</td><td style="text-align:left;"><strong>再次通知</strong>（状态从空→非空）</td></tr><tr><td style="text-align:left;"><strong>4. 你读 &quot;NewMsg&quot;</strong></td><td style="text-align:left;">只读到新数据</td><td style="text-align:left;">只读到新数据</td></tr></tbody></table></blockquote><table><thead><tr><th style="text-align:left;">特性</th><th style="text-align:left;">LT（水平触发）</th><th style="text-align:left;">ET（边缘触发）</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>通知频率</strong></td><td style="text-align:left;">持续通知（只要就绪）</td><td style="text-align:left;">仅状态变化时通知一次</td></tr><tr><td style="text-align:left;"><strong>编程难度</strong></td><td style="text-align:left;">简单</td><td style="text-align:left;">复杂（需非阻塞 + 循环 I/O）</td></tr><tr><td style="text-align:left;"><strong>数据安全性</strong></td><td style="text-align:left;">高（不怕漏处理）</td><td style="text-align:left;">低（漏处理 = 数据丢失）</td></tr><tr><td style="text-align:left;"><strong>性能潜力</strong></td><td style="text-align:left;">中</td><td style="text-align:left;">高（特定场景）</td></tr><tr><td style="text-align:left;"><strong>是否默认</strong></td><td style="text-align:left;">✅ 是（<code>epoll</code> 默认）</td><td style="text-align:left;">❌ 需显式开启</td></tr><tr><td style="text-align:left;"><strong>适用人群</strong></td><td style="text-align:left;">所有人</td><td style="text-align:left;">高级开发者</td></tr></tbody></table><blockquote><p><strong>Redis 在 LT 模式下（默认），对每个客户端连接：</strong></p><ol><li><strong>尽可能一次性读完 socket 接收缓冲区中的所有可用数据</strong>（通过非阻塞 I/O 循环读取）；</li><li><strong>将读到的数据放入客户端私有的 query buffer</strong>；</li><li><strong>然后逐条解析并执行其中的命令</strong>；</li><li><strong>不会重复执行命令，也不会因“旧数据残留”导致错误</strong>。</li></ol><p>通过<code>query buffer</code>，Redis是<strong>不会执行记录过的命令的</strong>，调用大致流程：</p><ol><li><strong>调用 <code>readQueryFromClient(client)</code></strong></li><li>在该函数内部： <ul><li>使用 <strong>非阻塞 socket</strong></li><li><strong>循环调用 <code>read()</code></strong>，直到返回 <code>EAGAIN</code>（表示暂时无更多数据）</li><li>将所有读到的数据 <strong>追加到 <code>client-&gt;querybuf</code>（SDS 字符串）</strong></li></ul></li><li>然后调用 <code>processInputBuffer(client)</code><ul><li>从 <code>querybuf</code> 中 <strong>按 Redis 协议（RESP）逐条解析命令</strong></li><li>每解析出一条完整命令，就立即执行（<code>call()</code>）</li><li>执行完后，<strong>从 <code>querybuf</code> 中移除已处理的部分</strong></li></ul></li></ol></blockquote><h2 id="io多路复用的流程" tabindex="-1"><a class="header-anchor" href="#io多路复用的流程"><span>IO多路复用的流程</span></a></h2><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">[Client] </span>
<span class="line">    │</span>
<span class="line">    ↓ (TCP 连接)</span>
<span class="line">[Web Server]</span>
<span class="line">├── 1. 创建监听 socket（listen_fd）</span>
<span class="line">├── 2. epoll_create() → 创建事件中心</span>
<span class="line">├── 3. epoll_ctl(ADD, listen_fd, EPOLLIN) → 监听连接事件</span>
<span class="line">└── 4. 事件循环：while (1) { epoll_wait() → 处理事件 }</span>
<span class="line">        ├── 若 listen_fd 就绪 → accept() 新连接</span>
<span class="line">        └── 若 client_fd 就绪 → read() → parse → execute → write()</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong><code>listen_fd</code>（监听 socket）就绪 ≠ 客户端请求数据就绪，而是表示“有新连接到达，可以调用 <code>accept()</code> 了”。</strong></p><p><strong>当有新连接产生时，<code>listen_fd</code>作为监听socket就会就绪，epoll接收到后就会尝试去通过accept获取连接信息建立连接（<code>listen_fd</code>本身不是存储连接信息的fd，单节点中全局唯一，就是个门铃）</strong></p><p><strong><code>accept()</code> 的作用是：从内核的“已完成连接队列”中取出一个新连接，并返回一个新的文件描述符（<code>client_fd</code>），用于后续与该客户端通信。</strong></p><p><strong>你不能直接用 <code>listen_fd</code> 读写客户端数据！必须通过 <code>accept()</code> 拿到 <code>client_fd</code> 才能通信。</strong></p></blockquote><p>当服务端调用 <code>listen(fd, backlog)</code> 后，内核会为该 socket 维护 <strong>两个队列</strong>：</p><table><thead><tr><th style="text-align:left;">队列</th><th style="text-align:left;">作用</th><th style="text-align:left;">状态</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>SYN 队列（半连接队列）</strong></td><td style="text-align:left;">存放收到 SYN 但未完成三次握手的连接</td><td style="text-align:left;"><code>SYN_RECV</code></td></tr><tr><td style="text-align:left;"><strong>Accept 队列（全连接队列）</strong></td><td style="text-align:left;">存放<strong>已完成三次握手</strong>的连接，等待 <code>accept()</code> 取走</td><td style="text-align:left;"><code>ESTABLISHED</code></td></tr></tbody></table><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">Client                    Server</span>
<span class="line">  │                         │</span>
<span class="line">  │-------- SYN -----------&gt;│ → 放入 SYN 队列（内核控制，非epoll控制）</span>
<span class="line">  │&lt;------- SYN+ACK --------│</span>
<span class="line">  │-------- ACK -----------&gt;│ → 移入 Accept 队列</span>
<span class="line">  │                         │</span>
<span class="line">  │                         │ ← listen_fd 变为“可读”</span>
<span class="line">  │                         │ ← 应用调用 accept()</span>
<span class="line">  │&lt;------ 新连接 fd --------│ → 从 Accept 队列取出，返回 client_fd</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>🔑 <strong>只有当 Accept 队列非空时，<code>listen_fd</code> 才会变为“可读”（即 <code>epoll_wait</code> 返回它）</strong>。</p></blockquote><p><code>listen_fd</code> 本质上是一个 <strong>监听 socket</strong>，它的唯一职责是：</p><blockquote><p><strong>“当有新 TCP 连接完成三次握手后，通知应用程序调用 <code>accept()</code> 来领取这个连接。”</strong></p></blockquote><p>它<strong>不存储任何具体客户端的信息</strong>，只维护两个队列：</p><ul><li><strong>SYN 队列</strong>（半连接）</li><li><strong>Accept 队列</strong>（已完成连接）</li></ul><p>但这些队列中的<strong>每个连接项</strong>，是由内核单独管理的，与 <code>listen_fd</code> 本身是分离的。</p><p>总结一下，我们寻常使用Redis时，会使用<strong>RedisClient</strong>去先<strong>连接Redis节点</strong>，在连接时，Redis节点内核会<strong>将<code>listen_fd</code>设置为就绪</strong>，epoll_wait在读出这条fd后尝试通过<strong>accept</strong>()到<strong>Accept队列</strong>中获取连接信息，<strong>得到<code>client_fd</code></strong>；</p><blockquote><p><strong><code>client_fd</code> 是通信管道，请求数据通过它流入 Redis 的接收缓冲区，再由协议解析器（RESP）解析成具体操作。</strong></p></blockquote><p>这个<code>client_fd</code>即与RedisClient的连接socket文件fd，当我们在<strong>RedisClient</strong>中向节点尝试<strong>发送命令调用</strong>时，<strong>Redis节点中对应的<code>client_fd</code>就会获取到详细的操作信息并被内核设置为就绪</strong>，等待epoll_wait调用<strong>解析 RESP 协议</strong>执行业务。</p><blockquote><p>Redis节点与客户端Client的连接会持续到一方下线为止，并不是Client执行一次命令，就tcp再建立一次连接</p></blockquote><blockquote><p><strong>补充：</strong></p><p><strong>I/O 多路复用是「用户空间程序」与「操作系统内核」之间的协作机制，用于高效监控多个文件描述符（fd）的 I/O 就绪状态。</strong></p><p>❌ <strong>它不是客户端与服务端之间的通信机制！</strong></p><p>但是我们确实可以使用它来完成客户端与服务端之间的<strong>连接的IO监控</strong>，就像是以上作为通信管道的<code>client_fd</code>。</p><p>用户空间程序：redis-server</p><p>操作系统内核：fd文件</p></blockquote><h2 id="信号驱动io" tabindex="-1"><a class="header-anchor" href="#信号驱动io"><span>信号驱动IO</span></a></h2><p>基本的非阻塞，系统调用后先返回结果，如无数据则内核检测数据，当数据生成后发送信号通知用户应用来获取数据（二阶段阻塞）</p><p>但是需要维护大量的表，可能栈溢出</p><p>内核与用户空间交互性能低</p><h2 id="异步io" tabindex="-1"><a class="header-anchor" href="#异步io"><span>异步IO</span></a></h2><p>完全的非阻塞，用户系统调用请求数据，内核等待数据并写到缓冲区，然后直接返回给用户</p><p>复杂度比较高，并发控制难度大</p><blockquote><p>只有二阶段是非阻塞（用户不需要阻塞等待业务执行）</p></blockquote><h2 id="redis的网络模型" tabindex="-1"><a class="header-anchor" href="#redis的网络模型"><span>Redis的网络模型</span></a></h2><h3 id="redis到底算不算是单线程" tabindex="-1"><a class="header-anchor" href="#redis到底算不算是单线程"><span>Redis到底算不算是单线程？</span></a></h3><p>只论业务处理（nosql命令处理），是的。</p><p>谈论整个Redis，其实是多线程（几个开销比较大的任务，为了不阻塞主线程，会额外开一个线程完成这些任务）</p><p>6.0中对客户端命令解析以及写响应结果的任务使用多线程实现</p>`,81)])])}const a=e(d,[["render",o]]),g=JSON.parse('{"path":"/docs/heimadianping/yuanlibaguwen/wangluomoxing.html","title":"Redis网络模型","lang":"en-US","frontmatter":{"title":"Redis网络模型","date":"2026-1-28"},"headers":[{"level":2,"title":"用户空间与内核空间","slug":"用户空间与内核空间","link":"#用户空间与内核空间","children":[]},{"level":2,"title":"阻塞IO（BIO）","slug":"阻塞io-bio","link":"#阻塞io-bio","children":[]},{"level":2,"title":"非阻塞IO（NIO）","slug":"非阻塞io-nio","link":"#非阻塞io-nio","children":[]},{"level":2,"title":"IO多路复用","slug":"io多路复用","link":"#io多路复用","children":[{"level":3,"title":"select","slug":"select","link":"#select","children":[]},{"level":3,"title":"poll","slug":"poll","link":"#poll","children":[]},{"level":3,"title":"epoll","slug":"epoll","link":"#epoll","children":[]}]},{"level":2,"title":"IO多路复用的事件通知机制（LT，ET）","slug":"io多路复用的事件通知机制-lt-et","link":"#io多路复用的事件通知机制-lt-et","children":[]},{"level":2,"title":"IO多路复用的流程","slug":"io多路复用的流程","link":"#io多路复用的流程","children":[]},{"level":2,"title":"信号驱动IO","slug":"信号驱动io","link":"#信号驱动io","children":[]},{"level":2,"title":"异步IO","slug":"异步io","link":"#异步io","children":[]},{"level":2,"title":"Redis的网络模型","slug":"redis的网络模型","link":"#redis的网络模型","children":[{"level":3,"title":"Redis到底算不算是单线程？","slug":"redis到底算不算是单线程","link":"#redis到底算不算是单线程","children":[]}]}],"git":{"createdTime":1771237270000,"updatedTime":1771237270000,"contributors":[{"name":"SaltFishGC","email":"130335482+SaltFishGC@users.noreply.github.com","commits":1}]},"filePathRelative":"docs/黑马点评/原理八股文/网络模型.md"}');export{a as comp,g as data};
