import{ar as n,as as e,ax as t,at as a}from"./app-BEAZiIq9.js";const l={};function i(o,s){return a(),e("div",null,[...s[0]||(s[0]=[t(`<h3 id="redis持久化" tabindex="-1"><a class="header-anchor" href="#redis持久化"><span>Redis持久化</span></a></h3><p>我们说数据持久化一般都是将数据存储到磁盘中，比方说数据库。而Redis也存在这么一种持久化，我们可以将内存中存储</p><h4 id="rdb持久化" tabindex="-1"><a class="header-anchor" href="#rdb持久化"><span>RDB持久化</span></a></h4><p>Redis会在<strong>固定时间点</strong>以及<strong>手动关机</strong>时进行<strong>数据快照</strong>，将内存中存储的数据<strong>持久化</strong>为<strong>rdb快照文件</strong>，将快照文件保存到硬盘中，当下次启动时会尝试从rdb文件中获取旧数据。</p><p>那么如何设置这个固定时间点呢？</p><p>我们可以在<strong>conf文件内</strong>对相关设置做配置：</p><p><strong>save设置rdb时间点：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">save <span class="token operator">&lt;</span>seconds<span class="token operator">&gt;</span> <span class="token operator">&lt;</span>changes<span class="token operator">&gt;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><ul><li>表示 <strong>在 <code>&lt;seconds&gt;</code> 秒内，如果有至少 <code>&lt;changes&gt;</code> 次写操作</strong>，就触发一次 <code>bgsave</code>（后台保存 RDB 快照）。</li><li>可以配置多条 <code>save</code> 规则，满足任意一条即触发快照。</li></ul><blockquote><p>假如不希望开启RDB，使用<code>save &quot;&quot;</code>显式禁用RDB或是注释掉所有save行</p></blockquote><p><strong>默认配置（Redis 官方默认值）：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">save <span class="token number">900</span> <span class="token number">1</span>      <span class="token comment"># 15分钟内至少有1次修改 → 触发快照</span></span>
<span class="line">save <span class="token number">300</span> <span class="token number">10</span>     <span class="token comment"># 5分钟内至少有10次修改 → 触发快照</span></span>
<span class="line">save <span class="token number">60</span> <span class="token number">10000</span>   <span class="token comment"># 1分钟内至少有10000次修改 → 触发快照</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>💡 这些规则是“<strong>或</strong>”的关系，只要满足其中任意一条，就会执行 <code>bgsave</code>。</p></blockquote><table><thead><tr><th style="text-align:left;">配置项</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><code>dbfilename dump.rdb</code></td><td style="text-align:left;">RDB 文件名，默认为 <code>dump.rdb</code></td></tr><tr><td style="text-align:left;"><code>dir ./</code></td><td style="text-align:left;">RDB 文件保存的目录（必须是可写目录）</td></tr><tr><td style="text-align:left;"><code>stop-writes-on-bgsave-error yes</code></td><td style="text-align:left;">如果 <code>bgsave</code> 失败，是否禁止写入（防止数据不一致）</td></tr><tr><td style="text-align:left;"><code>rdbcompression yes</code></td><td style="text-align:left;">是否对 RDB 文件启用 LZF 压缩（节省空间，默认开启）</td></tr><tr><td style="text-align:left;"><code>rdbchecksum yes</code></td><td style="text-align:left;">是否在 RDB 文件末尾添加 CRC64 校验（提高数据完整性，默认开启）</td></tr></tbody></table><blockquote><p>但是假如发生了宕机/断电等突发情况，单机模式下，数据是无法生成rdb快照文件的，那么这时候数据就会丢失！</p></blockquote><p><strong>那么bgsave是怎么实现的呢？</strong></p><p>我们说Redis是单线程服务，但是如果让主线程阻塞先完成数据的持久化好像不太行，如果在持久化时出现请求要求读写Redis数据就存在问题了，那么这时候我们就需要<strong>一个额外的线程</strong>来执行持久化操作了。</p><p>我们fork主线程得到一个子线程，子线程共享主线程的内存资源，就可以读取Redis在内存中的数据从而完成读写操作了。</p><p>但是我们知道为了保证资源安全，线程之间共享的资源是会上<strong>ReadOnly</strong>锁的，那么这时候假如有写入/修改需求怎么办？</p><p>事实上在持久化时，只读部分数据的需要修改部分会被创造一个副本用于被主线程执行读写操作，完成后覆盖。注意<strong>Redis 使用操作系统的「写时复制」（Copy-On-Write, COW）机制，只在需要时复制被修改的内存页，而不是一开始就复制全部数据。</strong></p><blockquote><p>这里子线程写入RDB文件的是只读部分的旧数据，而非新写入的数据。</p></blockquote><p>对于RDB，在两次写入之间如果发生宕机就会丢失数据。</p><h4 id="aof持久化" tabindex="-1"><a class="header-anchor" href="#aof持久化"><span>AOF持久化</span></a></h4><p>AOF（<strong>Append Only File</strong>）是 Redis 提供的一种<strong>持久化机制</strong>，通过记录服务器接收到的<strong>每一个写操作命令</strong>，以日志的形式追加到文件末尾。在 Redis 重启时，通过<strong>重放（replay）这些命令</strong>来恢复数据。</p><p><strong>对AOF的配置：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">appendonly <span class="token function">yes</span> <span class="token comment"># 开启AOF持久化</span></span>
<span class="line">appendfilename <span class="token string">&quot;appendonly.aof&quot;</span> <span class="token comment"># AOF文件.</span></span>
<span class="line">appendfsync everysec <span class="token comment"># 同步方案，默认everysec</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th style="text-align:left;">配置值</th><th style="text-align:left;">行为</th><th style="text-align:left;">数据安全性</th><th style="text-align:left;">性能</th><th style="text-align:left;">适用场景</th></tr></thead><tbody><tr><td style="text-align:left;"><code>always</code></td><td style="text-align:left;">每个写命令都 <code>fsync</code> 到磁盘</td><td style="text-align:left;">⭐⭐⭐ 几乎不丢数据</td><td style="text-align:left;">❌ 最差（每次写都要等磁盘 I/O）</td><td style="text-align:left;">对数据零丢失要求极高的场景（极少用）</td></tr><tr><td style="text-align:left;"><code>everysec</code></td><td style="text-align:left;">每秒 <code>fsync</code> 一次（<strong>默认</strong>）</td><td style="text-align:left;">⭐⭐ 最多丢 1 秒数据</td><td style="text-align:left;">✅ 平衡</td><td style="text-align:left;"><strong>绝大多数生产环境推荐</strong></td></tr><tr><td style="text-align:left;"><code>no</code></td><td style="text-align:left;">由操作系统决定何时写盘（通常 30 秒）</td><td style="text-align:left;">⭐ 可能丢几十秒数据</td><td style="text-align:left;">⚡ 最好</td><td style="text-align:left;">不关心数据丢失的缓存场景，不如RDB</td></tr></tbody></table><p><strong>AOF 的核心原理</strong></p><ol><li><strong>每当执行一个写命令</strong>（如 <code>SET</code>, <code>HSET</code>, <code>LPUSH</code> 等），Redis 就将该命令以 <strong>RESP 协议格式</strong>追加到 AOF 缓冲区。</li><li>根据配置的 <strong>同步策略（appendfsync）</strong>，将缓冲区内容写入并同步到磁盘上的 AOF 文件。</li><li><strong>Redis 重启时</strong>，会读取 AOF 文件，逐条执行其中的命令，重建原始数据集。</li></ol><blockquote><p>📌 AOF 文件是<strong>可读的文本文件</strong>，你可以用 <code>cat</code> 查看，甚至手动修复（但需谨慎）。</p></blockquote><p>示例 AOF 内容：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">*2</span>
<span class="line"> $ 6</span>
<span class="line">SELECT</span>
<span class="line"> $ 1</span>
<span class="line">0</span>
<span class="line">*3</span>
<span class="line"> $ 3</span>
<span class="line">SET</span>
<span class="line"> $ 3</span>
<span class="line">foo</span>
<span class="line"> $ 3</span>
<span class="line">bar</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>AOF 的问题：文件会无限增长！</strong></p><ul><li>每次 <code>INCR counter</code> 都会记录一条命令，即使最终值只是 +1。</li><li>删除 key 后，AOF 仍保留创建和删除的全部历史。</li><li>长期运行后，AOF 文件可能变得<strong>巨大且冗余</strong>。</li></ul><p><strong>解决方案：AOF 重写（Rewrite）</strong></p><p><strong>原理：</strong></p><ul><li><strong>不是重放旧命令</strong>，而是<strong>基于当前数据库状态，生成最简命令集</strong>。</li><li>例如：100 次 <code>INCR</code> → 直接 <code>SET counter 100</code></li><li>删除的 key 不会被写入新 AOF 文件。</li></ul><p><strong>触发方式：</strong></p><ul><li><p>手动：<code>BGREWRITEAOF</code> 命令</p></li><li><p>自动（配置）：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">auto-aof-rewrite-percentage <span class="token number">100</span> <span class="token comment"># 比上次文件增长超过100%发生重写</span></span>
<span class="line">auto-aof-rewrite-min-size 64mb <span class="token comment"># AOF文件最小多大才会触发重写</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>表示：当 AOF 文件比上次重写后增长超过 100% 且大于 64MB时，自动触发重写。</p></li></ul><p><strong>重写过程（关键！）：</strong></p><ol><li>主进程 <code>fork()</code> 出子进程。</li><li>子进程根据当前内存数据<strong>生成新的 AOF 文件</strong>（使用 COW，不影响主进程）。</li><li>主进程继续处理请求，并将<strong>新写命令同时写入旧 AOF 和 AOF 重写缓冲区</strong>。</li><li>子进程完成新 AOF 后，主进程将<strong>缓冲区中的增量命令追加到新文件</strong>。</li><li>原子替换旧 AOF 文件。</li></ol><blockquote><p>✅ 整个过程<strong>不阻塞主线程</strong>，保证服务可用性。</p></blockquote><h4 id="rdb以及aof的比较" tabindex="-1"><a class="header-anchor" href="#rdb以及aof的比较"><span>RDB以及AOF的比较</span></a></h4><table><thead><tr><th style="text-align:left;">对比维度</th><th style="text-align:left;">RDB</th><th style="text-align:left;">AOF</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>持久化方式</strong></td><td style="text-align:left;">定时快照<strong>数据</strong></td><td style="text-align:left;">实时<strong>命令</strong>日志</td></tr><tr><td style="text-align:left;"><strong>数据完整性</strong></td><td style="text-align:left;">较低（可能丢分钟级数据）</td><td style="text-align:left;">高（最多丢 1 秒）</td></tr><tr><td style="text-align:left;"><strong>恢复速度</strong></td><td style="text-align:left;">快</td><td style="text-align:left;">慢</td></tr><tr><td style="text-align:left;"><strong>文件大小</strong></td><td style="text-align:left;">小</td><td style="text-align:left;">大（可重写优化）</td></tr><tr><td style="text-align:left;"><strong>对性能影响</strong></td><td style="text-align:left;">低（后台 bgsave）</td><td style="text-align:left;">中（每秒 fsync）</td></tr><tr><td style="text-align:left;"><strong>可读性</strong></td><td style="text-align:left;">不可读（二进制）</td><td style="text-align:left;">可读（文本）</td></tr><tr><td style="text-align:left;"><strong>适合用途</strong></td><td style="text-align:left;">备份、<strong>快速</strong>恢复、缓存</td><td style="text-align:left;">高<strong>安全</strong>要求、主存储</td></tr><tr><td style="text-align:left;"><strong>是否默认开启</strong></td><td style="text-align:left;">是</td><td style="text-align:left;">否</td></tr></tbody></table><blockquote><p>高要求的复杂架构下<strong>不要二选一，而是“组合使用”！</strong></p></blockquote><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 同时开启 RDB 和 AOF</span></span>
<span class="line">save <span class="token number">900</span> <span class="token number">1</span></span>
<span class="line">save <span class="token number">300</span> <span class="token number">10</span></span>
<span class="line">appendonly <span class="token function">yes</span></span>
<span class="line">appendfsync everysec</span>
<span class="line">aof-use-rdb-preamble <span class="token function">yes</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><strong>RDB</strong> 用于：定期备份、快速冷启动、灾难恢复。</li><li><strong>AOF</strong> 用于：保证运行时数据不丢失。</li><li><strong>混合模式</strong> 让你鱼与熊掌兼得。</li></ul><h3 id="主从集群" tabindex="-1"><a class="header-anchor" href="#主从集群"><span>主从集群</span></a></h3><p>Redis 的<strong>主从集群（Master-Slave Replication）</strong> 是 Redis 提供的一种<strong>高可用、读写分离和数据冗余</strong>的基础架构。它通过将一个 Redis 实例（主节点）的数据<strong>异步复制</strong>到一个或多个从节点（Slave），实现故障转移、负载分担和数据备份。</p><table><thead><tr><th style="text-align:left;">目标</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>数据冗余</strong></td><td style="text-align:left;">主节点宕机后，从节点保留副本，避免数据丢失</td></tr><tr><td style="text-align:left;"><strong>读写分离</strong></td><td style="text-align:left;"><strong>写</strong>操作走<strong>主节点</strong>，<strong>读</strong>操作可分发到多个<strong>从节点</strong>，提升吞吐</td></tr><tr><td style="text-align:left;"><strong>高可用基础</strong></td><td style="text-align:left;">为哨兵（Sentinel）或 Redis Cluster 提供故障转移能力</td></tr><tr><td style="text-align:left;"><strong>灾备与备份</strong></td><td style="text-align:left;">从节点可作为冷备，用于数据恢复或分析</td></tr></tbody></table><p>在conf中完成配置：</p><p><strong>主节点通常不需要额外配置：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># redis-master.conf（可选，通常用默认配置即可）</span></span>
<span class="line">port <span class="token number">6379</span></span>
<span class="line"><span class="token builtin class-name">bind</span> <span class="token number">0.0</span>.0.0</span>
<span class="line">protected-mode no          <span class="token comment"># 生产环境建议用密码+防火墙替代</span></span>
<span class="line">daemonize <span class="token function">yes</span></span>
<span class="line">pidfile /var/run/redis_6379.pid</span>
<span class="line">logfile /var/log/redis/redis-server.log</span>
<span class="line"><span class="token function">dir</span> /var/lib/redis</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 主节点默认就是可写的，无需 replicaof 配置</span></span>
<span class="line"><span class="token comment"># 不要写 replicaof 或 slaveof！</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>🔔 <strong>主节点不能包含 <code>replicaof</code> 或 <code>slaveof</code> 指令</strong>，否则它会变成从节点！</p></blockquote><p><strong>从节点：</strong></p><p>关键在于指定主节点：<code>replicaof 192.168.1.100 6379</code></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 端口（避免与主节点冲突）</span></span>
<span class="line">port <span class="token number">6380</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 绑定地址</span></span>
<span class="line"><span class="token builtin class-name">bind</span> <span class="token number">0.0</span>.0.0</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 后台运行</span></span>
<span class="line">daemonize <span class="token function">yes</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 日志和 PID</span></span>
<span class="line">logfile /var/log/redis/redis-replica.log</span>
<span class="line">pidfile /var/run/redis_6380.pid</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 数据目录</span></span>
<span class="line"><span class="token function">dir</span> /var/lib/redis</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 关键：指定主节点 IP 和端口</span></span>
<span class="line">replicaof <span class="token number">192.168</span>.1.100 <span class="token number">6379</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 只读模式（默认开启，推荐保持）</span></span>
<span class="line">replica-read-only <span class="token function">yes</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 如果主节点有密码，需配置（重要！）</span></span>
<span class="line">masterauth your_master_password</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 如果使用 ACL（Redis 6+），可能需要 replicauser（较少用）</span></span>
<span class="line"><span class="token comment"># replica-announce-ip 192.168.1.101   # 用于 NAT/代理场景（可选）</span></span>
<span class="line"><span class="token comment"># replica-announce-port 6380</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>📌 <strong>注意</strong>：</p><ul><li>Redis 5.0 起，<code>slaveof</code> 已废弃，改用 <code>replicaof</code>。</li><li>如果主节点设置了密码（<code>requirepass</code>），<strong>必须配置 <code>masterauth</code></strong>，否则复制失败。</li></ul></blockquote><h3 id="主从节点的数据同步" tabindex="-1"><a class="header-anchor" href="#主从节点的数据同步"><span>主从节点的数据同步</span></a></h3><h4 id="全量同步" tabindex="-1"><a class="header-anchor" href="#全量同步"><span>全量同步</span></a></h4><p>Redis 的<strong>全量同步（Full Synchronization）</strong> 是主从复制（Replication）过程中，<strong>从节点（Replica）首次连接主节点（Master）或断连太久无法进行部分同步时，完整拷贝主节点当前全部数据的过程</strong>。它是 Redis 数据冗余和高可用架构的基础环节。</p><p><strong>发生全量同步的场景：</strong></p><table><thead><tr><th style="text-align:left;">场景</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>1. 从节点初次启动</strong></td><td style="text-align:left;">第一次连接主节点，没有历史数据</td></tr><tr><td style="text-align:left;"><strong>2. 从节点 runid 与主节点记录的不一致</strong></td><td style="text-align:left;">比如从节点重启后 runid 改变</td></tr><tr><td style="text-align:left;"><strong>3. 从节点请求的复制偏移量（offset）已不在主节点的复制积压缓冲区（Replication Backlog）中</strong></td><td style="text-align:left;">网络断开时间过长，主节点已覆盖旧命令</td></tr><tr><td style="text-align:left;"><strong>4. 执行 <code>REPLICAOF</code> 命令切换主节点</strong></td><td style="text-align:left;">重新建立复制关系</td></tr></tbody></table><blockquote><p>🔍 可通过 <code>INFO REPLICATION</code> 查看 <code>master_sync_in_progress:1</code> 判断是否正在全量同步。</p></blockquote><p>全量同步<strong>流程</strong>：</p><p><strong>步骤 1：从节点发起同步请求</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 从节点发送（初次同步）</span></span>
<span class="line">PSYNC ? <span class="token parameter variable">-1</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>?</code> 表示不知道主节点的 runid</li><li><code>-1</code> 表示没有有效偏移量</li></ul><hr><p><strong>步骤 2：主节点响应并准备 RDB 快照</strong></p><ul><li>主节点生成一个<strong>唯一的 runid</strong>（如果尚未生成）</li><li>主节点执行 <code>bgsave</code>，<strong>fork 子进程生成 RDB 文件</strong></li><li>同时，主节点开启一个<strong>复制客户端缓冲区（replication buffer）</strong>，缓存 <code>bgsave</code> 期间的所有写命令</li></ul><blockquote><p>⚠️ 此阶段主节点仍可正常处理读写请求（非阻塞），但 fork 和磁盘 I/O 会有性能影响。</p></blockquote><hr><p><strong>步骤 3：主节点发送 RDB 文件给从节点</strong></p><ul><li>RDB 文件通过 socket <strong>直接传输</strong>给从节点（支持无盘复制 <code>repl-diskless-sync</code>，但默认走磁盘）</li><li>从节点<strong>清空自身现有数据</strong>（如果有）</li></ul><hr><p><strong>步骤 4：从节点加载 RDB 文件</strong></p><ul><li>从节点将接收到的 RDB 文件<strong>加载到内存</strong>，重建数据库状态</li><li>此过程会<strong>阻塞从节点的主线程</strong>，期间无法响应客户端请求（但不影响主节点）</li></ul><hr><p><strong>步骤 5：主节点发送缓存的增量命令</strong></p><ul><li>RDB 传输完成后，主节点将<strong>复制缓冲区中积累的写命令</strong>发送给从节点</li><li>从节点<strong>依次执行这些命令</strong>，使数据状态与主节点完全一致</li></ul><hr><p><strong>步骤 6：进入稳定复制阶段</strong></p><ul><li>全量同步完成，主从进入<strong>命令传播（Command Propagation）</strong> 阶段</li><li>主节点每执行一个写命令，就立即发送给从节点（异步）</li></ul><p>**如何判断需要什么同步呢？**其实可以从slave节点传输的两个参数来判断：</p><p>一个是<strong>Replication Id/runid</strong>：数据集标记，id一致则说明是同一数据集，每一个主节点都有自己的id，从节点会继承主节点的id；</p><p>另一个是<strong>offset</strong>：偏移量，随着业务的运行，log文件会逐渐增长，需要记录最新的长度，主从节点的offset不一致，则需要进行同步；</p><p>那么主节点在收到同步请求的时候，就会对以上参数做判断：</p><p><strong>步骤 1：检查 runid 是否匹配</strong></p><ul><li><p>如果从节点发的是 <code>?</code> → <strong>runid 不匹配</strong> → <strong>必须全量同步</strong></p></li><li><p>如果从节点发的是具体 runid：</p><ul><li><p>与当前主节点的 <code>runid</code><strong>不一致</strong> → 全量同步</p><blockquote><p>（比如主节点重启过，runid 已变）</p></blockquote></li><li><p><strong>一致</strong> → 进入下一步</p></li></ul></li></ul><p><strong>步骤 2：检查 offset 是否在 backlog 范围内</strong></p><ul><li>主节点维护一个<strong>复制积压缓冲区（Replication Backlog）</strong>，记录最近的写命令及其偏移量范围 <code>[start_offset, end_offset]</code></li><li>如果从节点请求的 <code>offset</code> <strong>≥ start_offset</strong> → <strong>可以部分同步</strong></li><li>如果 <code>offset &lt; start_offset</code>（已被覆盖）→ <strong>必须全量同步</strong></li></ul><table><thead><tr><th style="text-align:left;">从节点发送的 PSYNC</th><th style="text-align:left;">主节点判断逻辑</th><th style="text-align:left;">结果</th></tr></thead><tbody><tr><td style="text-align:left;"><code>PSYNC ? -1</code></td><td style="text-align:left;">runid 未知</td><td style="text-align:left;"><strong>全量同步</strong>（典型初次同步）</td></tr><tr><td style="text-align:left;"><code>PSYNC &lt;旧runid&gt; &lt;任意offset&gt;</code></td><td style="text-align:left;">runid ≠ 当前主节点 runid</td><td style="text-align:left;"><strong>全量同步</strong>（主节点已重启）</td></tr><tr><td style="text-align:left;"><code>PSYNC &lt;当前runid&gt; &lt;有效offset&gt;</code></td><td style="text-align:left;">offset 在 backlog 中</td><td style="text-align:left;"><strong>部分同步</strong></td></tr><tr><td style="text-align:left;"><code>PSYNC &lt;当前runid&gt; &lt;过期offset&gt;</code></td><td style="text-align:left;">offset &lt; backlog 起始位置</td><td style="text-align:left;"><strong>全量同步</strong></td></tr></tbody></table><h4 id="增量同步" tabindex="-1"><a class="header-anchor" href="#增量同步"><span>增量同步</span></a></h4><p>如果都做全量同步，那么就会需要额外的资源来完成数据的同步，在一些情况下，我们其实可以只对主节点产生的新数据/命令进行加载而不需要完全清空从节点的数据，那么这时候我们就可以使用增量同步（部分同步）。</p><p><strong>当且仅当同时满足以下两个条件时，Redis 会使用增量同步：</strong></p><p><strong>条件 1：从节点提供的 <code>runid</code> 与当前主节点的 <code>runid</code> 一致</strong></p><ul><li>表示从节点之前确实是从<strong>当前这个主节点</strong>复制数据的。</li><li>如果主节点重启过（<code>runid</code> 改变），或从节点切换了主节点，则不满足。</li></ul><p><strong>条件 2：从节点请求的复制偏移量（offset）仍在主节点的「复制积压缓冲区」（Replication Backlog）范围内</strong></p><ul><li>主节点维护一个固定大小的环形缓冲区（默认 1MB），缓存最近的写命令及其偏移量。</li><li>如果从节点断连期间，主节点写入的数据量 <strong>没有超过 backlog 大小</strong>，则缺失的数据还在缓冲区中。</li></ul><blockquote><p>✅ 只有这两个条件都满足，主节点才会回复 <code>+CONTINUE</code>，并只发送缺失的命令（增量同步）。</p></blockquote><blockquote><p>简单记忆：</p><p>一、主节点id必须相同</p><p>二、丢失的更新记录log不能过多（主节点只记录一个环形缓冲区，不在其中则无法追溯）</p></blockquote><p>两者的比较：</p><table><thead><tr><th style="text-align:left;">特性</th><th style="text-align:left;">增量同步（Partial Sync）</th><th style="text-align:left;">全量同步（Full Sync）</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>触发条件</strong></td><td style="text-align:left;">runid 匹配 + offset 有效</td><td style="text-align:left;">否则</td></tr><tr><td style="text-align:left;"><strong>数据传输量</strong></td><td style="text-align:left;">仅缺失的写命令（KB～MB 级）</td><td style="text-align:left;">全量 RDB（GB 级）</td></tr><tr><td style="text-align:left;"><strong>主节点开销</strong></td><td style="text-align:left;">极低（仅读 backlog）</td><td style="text-align:left;">高（fork + bgsave + 网络 I/O）</td></tr><tr><td style="text-align:left;"><strong>从节点开销</strong></td><td style="text-align:left;">低（执行少量命令）</td><td style="text-align:left;">高（加载 RDB 阻塞主线程）</td></tr><tr><td style="text-align:left;"><strong>恢复时间</strong></td><td style="text-align:left;">毫秒～秒级</td><td style="text-align:left;">秒～分钟级（取决于数据量）</td></tr></tbody></table><h4 id="对同步的优化" tabindex="-1"><a class="header-anchor" href="#对同步的优化"><span>对同步的优化</span></a></h4><table><thead><tr><th>优化方向</th><th>配置项 / 措施</th><th>原理说明</th><th>优点</th><th>缺点 / 注意事项</th></tr></thead><tbody><tr><td>1. 无磁盘复制（Diskless Sync）</td><td><code>repl-diskless-sync yes</code> <code>repl-diskless-sync-delay 5</code></td><td>主节点不生成 RDB 文件到磁盘，而是通过 socket 直接将 RDB 数据流发送给从节点，避免磁盘 I/O。延迟可调（单位：秒），等待更多客户端连接后批量发送。</td><td>- 减少主节点磁盘压力 - 提升全量同步效率（尤其在高 I/O 负载时）</td><td>- 网络要求高（需稳定、低延迟） - 若网络中断，可能需要重传 - 不适合大文件或弱网环境</td></tr><tr><td>2. 控制单节点内存大小</td><td>限制单个 Redis 实例内存占用（如 &lt; 8GB）</td><td>内存越小，<code>bgsave</code> fork 和 RDB 生成越快，减少对主节点性能影响。</td><td>- 减少 fork 时间和内存开销 - 降低 RDB 持久化带来的磁盘 I/O 压力</td><td>- 单机容量有限，不适合超大规模数据 - 需结合分片（Cluster）扩展</td></tr><tr><td>3. 增大复制积压缓冲区（Backlog）</td><td><code>repl-backlog-size 256mb</code> 或更大</td><td>Backlog 是主节点维护的环形缓冲区，用于存储最近的写命令。增大其大小可以支持更长时间的断连恢复，提高部分同步成功率。</td><td>- 减少全量同步频率 - 快速恢复网络闪断后的数据一致性</td><td>- 占用主节点内存 - 不宜过大（建议根据写入量估算）</td></tr><tr><td>4. 限制从节点数量</td><td>控制每个 master 下的 replica 数量（建议 ≤ 5）</td><td>多个从节点同时请求同步时，主节点需处理多个连接和数据传输，增加 CPU 和网络负载。</td><td>- 减轻主节点压力 - 提高复制稳定性</td><td>- 可能导致读能力不足 - 可采用“主-从-从”链式结构缓解</td></tr><tr><td>5. 采用主-从-从链式结构（级联复制）</td><td>设置一个 slave 作为其他 slave 的 master（A → B → C）</td><td>将部分从节点连接到上游从节点，而不是直接连接主节点，减轻主节点的复制压力。</td><td>- 分摊主节点网络带宽 - 支持多层级复制，适合跨机房部署</td><td>- 增加复制延迟（级联层越多，延迟越高） - 不适合强一致性场景</td></tr><tr><td>6. 优化主节点配置</td><td><code>tcp-keepalive 300</code> <code>timeout 60</code></td><td>防止因网络空闲导致连接断开，减少不必要的全量同步。</td><td>- 保持长连接 - 提高复制稳定性</td><td>- 过长可能导致资源浪费</td></tr><tr><td>7. 合理设置复制缓冲区上限</td><td><code>client-output-buffer-limit replica 256mb 64mb 60</code></td><td>限制从节点输出缓冲区大小，防止 OOM。当缓冲区超过阈值时，主节点会关闭连接。</td><td>- 防止从节点卡住导致主节点阻塞 - 保护主节点稳定性</td><td>- 设置过小可能频繁断连 - 需根据网络状况调整</td></tr><tr><td>8. 使用混合持久化（Redis 4.0+）</td><td><code>aof-use-rdb-preamble yes</code></td><td>AOF 重写时使用 RDB 快照 + AOF 增量命令，重启时先加载 RDB（快），再执行 AOF 命令（准）。</td><td>- 加快从节点启动速度 - 提高数据安全性</td><td>- 仅适用于 AOF 开启的场景</td></tr></tbody></table><h3 id="哨兵机制" tabindex="-1"><a class="header-anchor" href="#哨兵机制"><span>哨兵机制</span></a></h3><p>Redis <strong>哨兵（Sentinel）</strong> 是 Redis 官方提供的<strong>高可用（High Availability, HA）解决方案</strong>，用于<strong>监控、自动故障检测和主从切换</strong>。当主节点（Master）宕机时，Sentinel 能自动将一个从节点（Replica）提升为新的主节点，并通知客户端更新连接地址，从而实现<strong>无人工干预的故障恢复</strong>。</p><blockquote><p>📌 哨兵不负责数据存储或分片，只负责<strong>管理主从集群的拓扑和状态</strong>。</p></blockquote><h4 id="功能及实现" tabindex="-1"><a class="header-anchor" href="#功能及实现"><span>功能及实现</span></a></h4><table><thead><tr><th style="text-align:left;">功能</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>1. 监控（Monitoring）</strong></td><td style="text-align:left;">持续检查主节点、从节点是否正常运行</td></tr><tr><td style="text-align:left;"><strong>2. 通知（Notification）</strong></td><td style="text-align:left;">当节点故障时，可通过 API 或脚本通知管理员</td></tr><tr><td style="text-align:left;"><strong>3. 自动故障转移（Automatic Failover）</strong></td><td style="text-align:left;">主节点宕机后，自动选举一个从节点升级为主，并重新配置其他从节点复制新主节点</td></tr><tr><td style="text-align:left;"><strong>4. 配置提供者（Configuration Provider）</strong></td><td style="text-align:left;">客户端（如 Lettuce）通过 Sentinel 获取当前主节点地址</td></tr></tbody></table><blockquote><ul><li><p>为什么至少 3 个从节点？</p><p>故障判定需要多数派投票（quorum），避免“脑裂”（Split-Brain）。</p><ul><li>1 个 Sentinel：无法判断是主挂了还是自己网络问题</li><li>2 个 Sentinel：可能各执一词（1:1 平票）</li><li><strong>3 个及以上</strong>：可达成多数共识（如 2/3 同意即判定故障）</li></ul></li></ul><p>💡 Sentinel 本身<strong>无状态</strong>，可部署在独立机器或与 Redis 实例共存（不推荐）。</p></blockquote><p>Sentinel是怎么做到确认某一节点下线/宕机的呢？实际上Sentinel节点会对其所配置的所有集群实例发送心跳包，当某实例未响应心跳包时，就会确认这一节点下线了：</p><p><strong>主观下线</strong>：某一Sentinel节点发送心跳包，发现一实例未响应自己的心跳包，该节点认为此实例<strong>主观下线</strong></p><p><strong>客观下线</strong>：若对某一实例的<strong>主观下线</strong>数量达到一定数量，则认定该实例<strong>客观下线</strong></p><p>倘若客观下线的是<strong>主节点</strong>，那么就需要从新选举一个从节点成为主节点：</p><p><strong>Leader 选举</strong></p><ul><li>所有 Sentinel 通过 <strong>Raft 算法</strong>选举一个 Leader（负责执行故障转移）</li></ul><p><strong>故障转移</strong></p><p>Leader Sentinel 执行：</p><ol><li>从从节点中<strong>选择一个最优的</strong>作为新主（优先级：<code>replica-priority</code> → 复制偏移量 → runid 字典序） <ol><li>根据<code>replica-priority</code>优先级选举新主节点，<code>replica-priority</code>在conf文件中给出，值越小优先级越高</li><li>如<code>replica-priority</code>均一致，则判断offset，offset越大说明从节点的同步数据记录更新，优先级越高</li><li>offset也一致，那么就通过<strong>从节点的runid</strong>随便挑一个（不是主节点的runid）</li></ol></li><li>向选中的从节点发送 <code>REPLICAOF NO ONE</code>，使其成为主</li><li>向其他从节点发送 <code>REPLICAOF &lt;new-master&gt;</code>，让它们复制新主</li><li><strong>更新主节点配置</strong>，并通知所有 Sentinel 和客户端</li></ol><p>最后会修改故障节点的配置，使之被新的主节点slave</p><h4 id="主从集群及哨兵配置" tabindex="-1"><a class="header-anchor" href="#主从集群及哨兵配置"><span>主从集群及哨兵配置</span></a></h4><p><strong>redis.conf</strong>:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 此为从节点，主节点无需额外配置</span></span>
<span class="line"><span class="token comment"># 端口（避免与主节点冲突）</span></span>
<span class="line">port <span class="token number">6380</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 绑定地址</span></span>
<span class="line"><span class="token builtin class-name">bind</span> <span class="token number">0.0</span>.0.0</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 后台运行</span></span>
<span class="line">daemonize <span class="token function">yes</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 日志和 PID</span></span>
<span class="line">logfile /var/log/redis/redis-replica.log</span>
<span class="line">pidfile /var/run/redis_6380.pid</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 数据目录</span></span>
<span class="line"><span class="token function">dir</span> /var/lib/redis</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 关键：指定主节点 IP 和端口</span></span>
<span class="line">replicaof <span class="token number">192.168</span>.1.100 <span class="token number">6379</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 只读模式（默认开启，推荐保持）</span></span>
<span class="line">replica-read-only <span class="token function">yes</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 如果主节点有密码，需配置（重要！）</span></span>
<span class="line">masterauth your_master_password</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 如果使用 ACL（Redis 6+），可能需要 replicauser（较少用）</span></span>
<span class="line"><span class="token comment"># replica-announce-ip 192.168.1.101   # 用于 NAT/代理场景（可选）</span></span>
<span class="line"><span class="token comment"># replica-announce-port 6380</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>sentinel.conf</strong>：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 假如需要自定义ip</span></span>
<span class="line"><span class="token comment"># sentinel announce-ip 192.168.1.101</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 启动端口号，默认26379</span></span>
<span class="line">port <span class="token number">26380</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 监控名为 mymaster 的主节点（IP + 端口）</span></span>
<span class="line"><span class="token comment"># quorum = 2 表示至少 2 个 Sentinel 同意才判定客观下线</span></span>
<span class="line">sentinel monitor mymaster <span class="token number">192.168</span>.1.100 <span class="token number">6379</span> <span class="token number">2</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 主节点密码（如果设置了 requirepass）</span></span>
<span class="line">sentinel auth-pass mymaster your_redis_password</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 判定主观下线时间（毫秒）</span></span>
<span class="line">sentinel down-after-milliseconds mymaster <span class="token number">30000</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 故障转移超时（毫秒）</span></span>
<span class="line">sentinel failover-timeout mymaster <span class="token number">180000</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 并行同步从节点数量（避免主节点压力过大）</span></span>
<span class="line">sentinel parallel-syncs mymaster <span class="token number">1</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 通知脚本（可选）</span></span>
<span class="line">sentinel notification-script mymaster /path/to/notify.sh</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>⚠️ <strong>每个 Sentinel 必须配置相同的 <code>mymaster</code> 名称</strong>，才能协同工作.</p></blockquote><p>完成sentinel的配置后，我们还需要去执行命令以启动sentinel服务：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">redis-sentinel path/to/sentinel.conf</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><strong>SpringBoot中的Redis配置：</strong></p><p>依赖只需要引入自带的data-redis即可，其内部包含了lettuce：</p><div class="language-xml line-numbers-mode" data-highlighter="prismjs" data-ext="xml" data-title="xml"><pre><code><span class="line">    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>dependency</span><span class="token punctuation">&gt;</span></span></span>
<span class="line">        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">&gt;</span></span>org.springframework.boot<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">&gt;</span></span></span>
<span class="line">        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">&gt;</span></span>spring-boot-starter-data-redis<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">&gt;</span></span></span>
<span class="line">    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>dependency</span><span class="token punctuation">&gt;</span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><code>spring-boot-starter-data-redis</code> 内部默认依赖 <strong>Lettuce</strong>（非 Jedis）</p></blockquote><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token comment"># application.yml</span></span>
<span class="line"><span class="token key atrule">spring</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">redis</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">sentinel</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token key atrule">master</span><span class="token punctuation">:</span> mymaster               <span class="token comment"># Sentinel 中定义的主节点名称，注意这里只用连接sentinel就行，Redis不配置</span></span>
<span class="line">      <span class="token key atrule">nodes</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.10<span class="token punctuation">:</span><span class="token number">26379</span>		 <span class="token comment"># sentinel的ip以及端口，通过sentinel发现Redis</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.11<span class="token punctuation">:</span><span class="token number">26379</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.12<span class="token punctuation">:</span><span class="token number">26379</span></span>
<span class="line">    <span class="token key atrule">password</span><span class="token punctuation">:</span> your_password          <span class="token comment"># 如果有密码</span></span>
<span class="line">    <span class="token key atrule">lettuce</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token key atrule">pool</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token key atrule">max-active</span><span class="token punctuation">:</span> <span class="token number">8</span></span>
<span class="line">        <span class="token key atrule">max-idle</span><span class="token punctuation">:</span> <span class="token number">4</span></span>
<span class="line">        <span class="token key atrule">min-idle</span><span class="token punctuation">:</span> <span class="token number">1</span></span>
<span class="line">    <span class="token comment"># 👇 关键：开启读写分离</span></span>
<span class="line">    <span class="token key atrule">read-from</span><span class="token punctuation">:</span> REPLICA_PREFERRED     <span class="token comment"># 优先从从节点读</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th style="text-align:left;">策略</th><th style="text-align:left;">说明</th><th style="text-align:left;">适用场景</th></tr></thead><tbody><tr><td style="text-align:left;"><strong><code>MASTER</code></strong></td><td style="text-align:left;">所有读写都走<strong>主节点</strong></td><td style="text-align:left;">- 默认策略 - 强一致性要求 - 无从节点环境</td></tr><tr><td style="text-align:left;"><strong><code>MASTER_PREFERRED</code></strong></td><td style="text-align:left;">优先读主，主不可用时<strong>自动降级读从</strong></td><td style="text-align:left;">- 兼顾一致性与可用性 - 主节点通常负载不高</td></tr><tr><td style="text-align:left;"><strong><code>REPLICA</code></strong></td><td style="text-align:left;"><strong>强制只读从节点</strong>，主节点不处理任何读请求</td><td style="text-align:left;">- 主节点压力极大 - 业务容忍数据延迟</td></tr><tr><td style="text-align:left;"><strong><code>REPLICA_PREFERRED</code></strong></td><td style="text-align:left;"><strong>优先读从节点</strong>，从不可用时<strong>回退到主节点</strong></td><td style="text-align:left;">✅ <strong>生产环境最推荐</strong> - 提升读吞吐 - 自动容错</td></tr></tbody></table><p>接下来就可以直接同之前的<code>StringRedisTemplate</code>一样用了。</p><h3 id="分片集群" tabindex="-1"><a class="header-anchor" href="#分片集群"><span>分片集群</span></a></h3><p>在主从集群中，我们还是没能解决两个问题：<strong>高并发写</strong>，<strong>海量数据的存储</strong>；</p><p>对于写入操作，我们还是靠单节点完成，甚至需要额外的资源开销以完成与从节点的数据同步；</p><p>对于海量数据的存储，我们是完全复制给所有节点而并非分摊压力；</p><p>所以我们还是需要在此基础之上完成更进一步的优化，也即解决数据存储压力与请求压力的缓解。Redis其实就推出了一个方案以解决这样的问题，也就是分片集群（Redis Cluster）：</p><h4 id="概念与实现原理" tabindex="-1"><a class="header-anchor" href="#概念与实现原理"><span>概念与实现原理</span></a></h4><p>Redis <strong>分片集群（Redis Cluster）</strong> 是 Redis 官方提供的<strong>分布式解决方案</strong>，用于解决单机 Redis 的 <strong>容量瓶颈（内存限制）</strong> 和 <strong>性能瓶颈（单节点 QPS 限制）</strong>。它通过<strong>数据分片（Sharding） + 高可用（主从复制 + 自动故障转移）</strong> 实现水平扩展。</p><table><thead><tr><th style="text-align:left;">特性</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>数据分片（Sharding）</strong></td><td style="text-align:left;">数据按 <strong>16384 个哈希槽（Hash Slot）</strong> 分布到多个节点</td></tr><tr><td style="text-align:left;"><strong>去中心化</strong></td><td style="text-align:left;">无中心节点，所有节点平等通信（Gossip 协议）</td></tr><tr><td style="text-align:left;"><strong>高可用</strong></td><td style="text-align:left;">每个分片支持 <strong>1 主 N 从</strong>，主挂后自动选举从节点接替</td></tr><tr><td style="text-align:left;"><strong>客户端直连</strong></td><td style="text-align:left;">客户端直接连接任意节点，<strong>自动重定向</strong>到正确节点</td></tr><tr><td style="text-align:left;"><strong>在线扩缩容</strong></td><td style="text-align:left;">支持动态添加/删除节点，<strong>数据自动迁移</strong></td></tr></tbody></table><p>主要特征为：</p><ul><li>集群多主节点，不同主节点存储<strong>不同数据</strong>；</li><li>每个主节点有多个从节点；</li><li>主节点之间通过心跳包检测彼此节点的健康状态（替代哨兵方案的sentinel检测实例）；</li><li>客户端访问任意节点，最终都会被转接到正确节点；</li></ul><h4 id="高可用方案" tabindex="-1"><a class="header-anchor" href="#高可用方案"><span>高可用方案</span></a></h4><p>我们先前在sentinel中使用额外的sentinel哨兵检测集群实例是否正常运行，假如主节点没有正常运行则会推举一个sentinel来做新的主节点推举，其会从该主节点的从节点中推举一个成为新的主节点。</p><p>而在分片集群中，<strong>多个主节点</strong>互相之间也起到了类似<strong>sentinel哨兵</strong>的作用，正如我们先前提到的互相检测实例运行状态，其会不断检测批次状态，一个发现即为<strong>主观下线</strong>，超过一定阈值的主观发现即为<strong>客观下线</strong>。</p><p>当发现一个<strong>主节点客观下线</strong>时，就会<strong>推举一个从节点</strong>（类似哨兵的推举机制Raft）成为主节点，并<strong>广播通知全部节点更新拓扑结构</strong>，下次前端请求就会<strong>重定向</strong>到新的主节点。</p><table><thead><tr><th style="text-align:left;">问题</th><th style="text-align:left;">答案</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>Cluster 主节点挂了怎么办？</strong></td><td style="text-align:left;"><strong>自动故障转移</strong>（无需哨兵）</td></tr><tr><td style="text-align:left;"><strong>需要哨兵吗？</strong></td><td style="text-align:left;">❌ <strong>不需要</strong>！Cluster 内置高可用</td></tr><tr><td style="text-align:left;"><strong>关键前提？</strong></td><td style="text-align:left;">每个主节点必须配 <strong>至少 1 个从节点</strong></td></tr><tr><td style="text-align:left;"><strong>切换时间？</strong></td><td style="text-align:left;">约 <code>cluster-node-timeout</code>（默认 15 秒） + 选举时间</td></tr></tbody></table><h4 id="分片集群配置" tabindex="-1"><a class="header-anchor" href="#分片集群配置"><span>分片集群配置</span></a></h4><p><strong>每个Redis节点的conf配置：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 端口（每个节点不同）</span></span>
<span class="line">port <span class="token number">7000</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 绑定地址（生产环境建议指定内网 IP）</span></span>
<span class="line"><span class="token builtin class-name">bind</span> <span class="token number">0.0</span>.0.0</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 后台运行</span></span>
<span class="line">daemonize <span class="token function">yes</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># PID 文件（避免冲突）</span></span>
<span class="line">pidfile /var/run/redis_7000.pid</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 日志文件</span></span>
<span class="line">logfile /var/log/redis/redis-7000.log</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 数据目录（每个节点独立）</span></span>
<span class="line"><span class="token function">dir</span> /var/lib/redis/7000</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 👇 关键：启用集群模式</span></span>
<span class="line">cluster-enabled <span class="token function">yes</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 集群节点配置文件（自动生成，不要手动编辑）</span></span>
<span class="line">cluster-config-file nodes-7000.conf</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 节点超时时间（毫秒），影响故障检测速度</span></span>
<span class="line">cluster-node-timeout <span class="token number">15000</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 是否开启 AOF（推荐开启，提高数据安全性）</span></span>
<span class="line">appendonly <span class="token function">yes</span></span>
<span class="line">appendfilename <span class="token string">&quot;appendonly.aof&quot;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 如果主节点有密码（所有节点密码必须一致！）</span></span>
<span class="line">requirepass your_redis_password</span>
<span class="line">masterauth your_redis_password  <span class="token comment"># 从节点连接主节点时用</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 客户端输出缓冲区限制（防 OOM）</span></span>
<span class="line">client-output-buffer-limit normal <span class="token number">0</span> <span class="token number">0</span> <span class="token number">0</span></span>
<span class="line">client-output-buffer-limit replica 256mb 64mb <span class="token number">60</span></span>
<span class="line">client-output-buffer-limit pubsub 32mb 8mb <span class="token number">60</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>连接集群中的Redis</strong></p><p>在使用 <code>redis.conf</code> <strong>启动</strong> Redis Cluster 节点后，<strong>节点只是“启动了”，但彼此尚未组成集群</strong>。还需要通过 <strong><code>redis-cli --cluster create</code></strong> 命令手动创建集群，并分配哈希槽（hash slots），之后才能确认主从关系。</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">redis-cli <span class="token parameter variable">--cluster</span> create <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token operator">&lt;</span>ip1:port<span class="token operator"><span class="token file-descriptor important">1</span>&gt;</span> <span class="token operator">&lt;</span>ip2:port<span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span> <span class="token punctuation">..</span>. <span class="token punctuation">\\</span></span>
<span class="line">  --cluster-replicas <span class="token operator">&lt;</span>N<span class="token operator">&gt;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>这条命令会自动完成：</strong></p><ul><li>向每个节点发送 <code>CLUSTER MEET</code>，让它们互相发现，因此只需要在一个节点执行此命令即可</li><li>分配哈希槽（16384 个）给主节点</li><li>指定从节点复制哪个主节点</li><li>更新每个节点的 <code>nodes-*.conf</code> 文件</li></ul><p><strong>示例：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">redis-cli <span class="token parameter variable">--cluster</span> create <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token number">127.0</span>.0.1:7000 <span class="token number">127.0</span>.0.1:7001 <span class="token number">127.0</span>.0.1:7002 <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token number">127.0</span>.0.1:7003 <span class="token number">127.0</span>.0.1:7004 <span class="token number">127.0</span>.0.1:7005 <span class="token punctuation">\\</span></span>
<span class="line">  --cluster-replicas <span class="token number">1</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><ul><li><code>--cluster-replicas 1</code> 表示：<strong>每个主节点配 1 个从节点</strong></li><li>命令会自动： <ol><li>将前 3 个节点设为主</li><li>将后 3 个节点设为从</li><li>将 16384 个哈希槽平均分配给 3 个主节点</li></ol></li></ul></blockquote><p><code>redis-cli --cluster create</code> 命令会 <strong>根据 <code>--cluster-replicas N</code> 参数，自动计算需要多少个主节点，并从你提供的节点列表中选出主节点和从节点</strong>。</p><p>假设你提供了 <strong>M 个节点</strong>，并指定 <code>--cluster-replicas N</code>（即每个主节点配 N 个从节点），那么：</p><ul><li><strong>所需主节点数</strong> = <code>M / (N + 1)</code></li><li><strong>必须满足</strong>：<code>M % (N + 1) == 0</code>，否则报错</li></ul><p><strong>SpringBoot中的配置：</strong></p><p>data-redis原生自带对于Cluster的支持，引入即可：</p><div class="language-xml line-numbers-mode" data-highlighter="prismjs" data-ext="xml" data-title="xml"><pre><code><span class="line">    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>dependency</span><span class="token punctuation">&gt;</span></span></span>
<span class="line">        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">&gt;</span></span>org.springframework.boot<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">&gt;</span></span></span>
<span class="line">        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">&gt;</span></span>spring-boot-starter-data-redis<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">&gt;</span></span></span>
<span class="line">    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>dependency</span><span class="token punctuation">&gt;</span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>yml中对于细节的设置：</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token key atrule">spring</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">redis</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token comment"># ❌ 不要配置 host/port（Cluster 模式不用）</span></span>
<span class="line">    <span class="token comment"># host: localhost</span></span>
<span class="line">    <span class="token comment"># port: 6379</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment"># ✅ 配置集群节点列表（至少包含一个主节点）</span></span>
<span class="line">    <span class="token key atrule">cluster</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token key atrule">nodes</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.10<span class="token punctuation">:</span><span class="token number">7000</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.11<span class="token punctuation">:</span><span class="token number">7001</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.12<span class="token punctuation">:</span><span class="token number">7002</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.13<span class="token punctuation">:</span><span class="token number">7003</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.14<span class="token punctuation">:</span><span class="token number">7004</span></span>
<span class="line">        <span class="token punctuation">-</span> 192.168.1.15<span class="token punctuation">:</span><span class="token number">7005</span></span>
<span class="line"></span>
<span class="line">      <span class="token comment"># 最大重定向次数（默认 5）</span></span>
<span class="line">      <span class="token key atrule">max-redirects</span><span class="token punctuation">:</span> <span class="token number">3</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment"># 如果 Redis 有密码</span></span>
<span class="line">    <span class="token key atrule">password</span><span class="token punctuation">:</span> your_redis_password</span>
<span class="line"></span>
<span class="line">    <span class="token comment"># 连接池配置（需要 commons-pool2 依赖）</span></span>
<span class="line">    <span class="token key atrule">lettuce</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token key atrule">pool</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token key atrule">max-active</span><span class="token punctuation">:</span> <span class="token number">8</span>      <span class="token comment"># 最大连接数</span></span>
<span class="line">        <span class="token key atrule">max-idle</span><span class="token punctuation">:</span> <span class="token number">4</span>        <span class="token comment"># 最大空闲连接</span></span>
<span class="line">        <span class="token key atrule">min-idle</span><span class="token punctuation">:</span> <span class="token number">1</span>        <span class="token comment"># 最小空闲连接</span></span>
<span class="line">        <span class="token key atrule">max-wait</span><span class="token punctuation">:</span> 2000ms   <span class="token comment"># 获取连接最大等待时间</span></span>
<span class="line"></span>
<span class="line">      <span class="token comment"># 集群拓扑刷新策略（可选）</span></span>
<span class="line">      <span class="token key atrule">cluster</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token comment"># 自动刷新集群拓扑（应对节点变更）</span></span>
<span class="line">        <span class="token key atrule">refresh-period</span><span class="token punctuation">:</span> 30s</span>
<span class="line"></span>
<span class="line">        <span class="token comment"># 读写分离策略（推荐）（先读从再读主）</span></span>
<span class="line">        <span class="token key atrule">read-from</span><span class="token punctuation">:</span> REPLICA_PREFERRED</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="散列插槽" tabindex="-1"><a class="header-anchor" href="#散列插槽"><span>散列插槽</span></a></h4><p>Redis <strong>哈希槽（Hash Slot）</strong> 是 Redis Cluster 实现<strong>数据分片（Sharding）</strong> 的核心机制。它将整个 key 空间划分为 <strong>16384 个固定槽位</strong>，每个 key 通过哈希算法映射到其中一个槽，而每个槽由一个主节点负责。</p><p><strong>为什么是 16384 个槽？</strong></p><p>这是 Redis 作者精心设计的数字，原因如下：</p><table><thead><tr><th style="text-align:left;">原因</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>1. 节点通信开销小</strong></td><td style="text-align:left;">每个节点需广播自己负责的槽位（bitmap）。 16384 位 = 2KB，网络传输成本低。</td></tr><tr><td style="text-align:left;"><strong>2. 足够细粒度</strong></td><td style="text-align:left;">即使有 1000 个节点，平均每个节点也有 16 个槽，负载较均衡。</td></tr><tr><td style="text-align:left;"><strong>3. 兼容性好</strong></td><td style="text-align:left;">CRC16 哈希结果为 16 位（最大 65536），<code>65536 / 4 = 16384</code>，计算高效。</td></tr></tbody></table><blockquote><p>💡 如果设为 65536 个槽：</p><ul><li>每个节点状态信息需 8KB（65536/8）</li><li>1000 节点集群 → 心跳包过大，Gossip 协议效率下降</li></ul></blockquote><p><strong>key如何映射到hash插槽中？</strong></p><p><strong>计算公式：</strong></p><div class="language-java line-numbers-mode" data-highlighter="prismjs" data-ext="java" data-title="java"><pre><code><span class="line">slot <span class="token operator">=</span> <span class="token function">CRC16</span><span class="token punctuation">(</span>key<span class="token punctuation">)</span> <span class="token operator">%</span> <span class="token number">16384</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><ul><li><code>CRC16()</code>：Redis 使用的 16 位循环冗余校验算法（标准实现）</li><li><code>% 16384</code>：取模运算，确保结果在 <code>[0, 16383]</code> 范围内</li></ul><p><strong>哈希槽与节点的关系</strong></p><p><strong>1. 初始分配</strong></p><ul><li>创建集群时（<code>redis-cli --cluster create</code>），16384 个槽<strong>平均分配给所有主节点</strong>。</li><li>例如 3 主节点： <ul><li>Node A：槽 0 ～ 5460（共 5461 个）</li><li>Node B：槽 5461 ～ 10922（共 5462 个）</li><li>Node C：槽 10923 ～ 16383（共 5461 个）</li></ul></li></ul><p><strong>2.动态迁移</strong></p><ul><li>扩容时：从现有主节点<strong>迁移部分槽</strong>到新主节点</li><li>缩容时：将待下线节点的槽<strong>迁移到其他主节点</strong></li></ul><blockquote><p>⚡ 迁移过程对客户端透明（通过 <code>ASK</code> 重定向临时转发请求）</p></blockquote><p>这样一来我们就可以只管给key和value，而不是每次在redisClient操作都需要指定某一个主节点；key被hash后就只会导向<strong>一个hash值</strong>，交由<strong>负责该hash值/插槽</strong>的<strong>主节点和他的从节点</strong>负责，这个传递的过程：</p><p>当客户端尝试向<strong>随便一个redis节点</strong>发送请求时，就会对key作hash得到hash插槽结果：</p><p>若正好该hash插槽就是由该节点负责，则直接处理命令；</p><p>若不是该节点负责，则节点返回重定向错误，客户端（如 Lettuce）自动：</p><ol><li>更新本地集群拓扑缓存</li><li>重连到 <code>192.168.1.10:7001</code></li><li>重新发送命令</li></ol><p>当然一般客户端是会有拓扑缓存的，现代 Redis 客户端（如 <strong>Lettuce</strong>、<strong>Jedis 4.x+</strong>）在连接 Redis Cluster 时，<strong>会主动缓存集群拓扑（Cluster Topology）</strong>，包括：</p><ul><li>每个主节点负责的 <strong>哈希槽范围（slot ranges）</strong></li><li>所有节点的 <strong>IP + 端口 + 角色（主/从）</strong></li><li>节点的 <strong>唯一 ID（node id）</strong></li></ul><blockquote><p><strong>插槽与主节点的映射实现：</strong></p><p><strong>Lettuce</strong>通过维护一个**<code>SlotOwnership</code>** 数组记录每个插槽所对应的主节点来实现映射关系的记录：</p><div class="language-java line-numbers-mode" data-highlighter="prismjs" data-ext="java" data-title="java"><pre><code><span class="line"><span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">int</span> slot <span class="token operator">=</span> start<span class="token punctuation">;</span> slot <span class="token operator">&lt;=</span> end<span class="token punctuation">;</span> slot<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    slotOwners<span class="token punctuation">[</span>slot<span class="token punctuation">]</span> <span class="token operator">=</span> masterNode<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>查询时：<code>RedisClusterNode node = slotOwners[slot];</code> → <strong>O(1)</strong></p></blockquote><p><strong>那么如何保存同一类数据到指定Redis实例（主节点及其从节点）呢？</strong></p><p><strong>使用 Hash Tag（最常用、最推荐）</strong></p><p>Redis Cluster 提供了 <strong>Hash Tag 机制</strong>：</p><blockquote><p><strong>用 <code>{}</code> 包裹 key 的一部分，集群只对 <code>{}</code> 内的内容计算哈希槽。</strong></p></blockquote><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token punctuation">{</span>tag<span class="token punctuation">}</span>rest_of_key</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><ul><li>只有 <code>tag</code> 部分参与 <code>CRC16</code> 计算</li><li><code>rest_of_key</code> 不影响槽位</li></ul><p><strong>📌 示例：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 这三个 key 会落在同一个槽（因为 tag 都是 &quot;user1001&quot;）</span></span>
<span class="line">user:<span class="token punctuation">{</span>user1001<span class="token punctuation">}</span>:profile</span>
<span class="line">user:<span class="token punctuation">{</span>user1001<span class="token punctuation">}</span>:orders</span>
<span class="line">user:<span class="token punctuation">{</span>user1001<span class="token punctuation">}</span>:settings</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 即使内容不同，只要 { } 内相同，就在同一槽</span></span>
<span class="line">log:<span class="token punctuation">{</span>order_20240501<span class="token punctuation">}</span>:part1</span>
<span class="line">log:<span class="token punctuation">{</span>order_20240501<span class="token punctuation">}</span>:part2</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>✅ 优势：</strong></p><ul><li><strong>天然支持多 Key 操作</strong>：<code>MGET</code>、<code>MSET</code>、事务、Lua 脚本</li><li><strong>业务语义清晰</strong>：按实体 ID 分组</li><li><strong>客户端无需特殊处理</strong></li></ul><blockquote><p>💡 这是 Redis 官方推荐的“同节点存储”方案。</p></blockquote><h4 id="集群动态伸缩" tabindex="-1"><a class="header-anchor" href="#集群动态伸缩"><span>集群动态伸缩</span></a></h4><p><strong>加入新的从节点：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">redis-cli <span class="token parameter variable">--cluster</span> add-node <span class="token punctuation">\\</span></span>
<span class="line">  --cluster-slave <span class="token punctuation">\\</span></span>
<span class="line">  --cluster-master-id <span class="token operator">&lt;</span><span class="token number">7000</span>的node-id<span class="token operator">&gt;</span> <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token number">192.168</span>.1.10:7006 <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token number">192.168</span>.1.10:7000</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>如何获取</strong> <code>&lt;7000的node-id&gt;</code><strong>？</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">redis-cli <span class="token parameter variable">-p</span> <span class="token number">7000</span> CLUSTER NODES</span>
<span class="line"><span class="token comment"># 输出示例：</span></span>
<span class="line"><span class="token comment"># a1b2c3d4... 127.0.0.1:7000@17000 master - 0 123456 1 connected 0-5460</span></span>
<span class="line"><span class="token comment"># ↑ node-id = a1b2c3d4...</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>✅ 执行后，7006 自动开始复制 7000 的数据。</p></blockquote><p><strong>加入新的主节点：</strong></p><p>当我们需要向正在运行的集群中加入新的节点，如果是主节点要怎么给他<strong>分配插槽</strong>呢？并且分配了对应的插槽后，又要怎么<strong>迁移对应的数据</strong>呢？</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">redis-cli <span class="token parameter variable">--cluster</span> add-node <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token number">192.168</span>.1.10:7006 <span class="token punctuation">\\</span>    <span class="token comment"># 新节点地址</span></span>
<span class="line">  <span class="token number">192.168</span>.1.10:7000      <span class="token comment"># 集群中任意现有节点（用于发现全集群）</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>新节点（7006）向 7000 发送 <code>CLUSTER MEET</code></li><li>7000 将全集群拓扑广播给 7006</li><li><strong>所有节点互相认识</strong>，但新节点<strong>没有分配任何哈希槽</strong>（角色为 <code>master</code>，但无数据）</li></ul><blockquote><p>✅ 此时新节点已属于集群，但处于“空闲主节点”状态。</p></blockquote><p><strong>步骤 1：迁移哈希槽到新主节点</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">redis-cli <span class="token parameter variable">--cluster</span> reshard <span class="token number">192.168</span>.1.10:7000</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>交互式操作：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">How many slots <span class="token keyword">do</span> you want to move? <span class="token number">4096</span>          <span class="token comment"># 迁移 4096 个槽（16384/4）</span></span>
<span class="line">What is the receiving <span class="token function">node</span> ID? <span class="token operator">&lt;</span><span class="token number">7006</span>的node-id<span class="token operator">&gt;</span>    <span class="token comment"># 新主节点 ID</span></span>
<span class="line">Please enter all the <span class="token builtin class-name">source</span> <span class="token function">node</span> IDs.			  <span class="token comment"># 挑选被拆出hash插槽的节点id，推荐all使得hash结果均衡，节点负载均衡</span></span>
<span class="line">  Type <span class="token string">&#39;all&#39;</span> to use all the nodes as <span class="token builtin class-name">source</span> nodes <span class="token keyword">for</span> the <span class="token builtin class-name">hash</span> slots.</span>
<span class="line">  Type <span class="token string">&#39;done&#39;</span> once you entered all the <span class="token builtin class-name">source</span> nodes: all</span>
<span class="line">Do you want to proceed with the proposed reshard plan? <span class="token function">yes</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>💡 建议：</p><ul><li>迁移数量 = <code>16384 / (原主数 + 1)</code>（如 3→4 主，迁 4096）</li><li>源节点选 <code>all</code> 让负载更均衡</li></ul></blockquote><p><strong>步骤 2：验证迁移结果</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">redis-cli <span class="token parameter variable">-p</span> <span class="token number">7006</span> CLUSTER NODES</span>
<span class="line"><span class="token comment"># 应看到 7006 负责部分槽，如 &quot;connected 12288-16383&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>⚡ 迁移过程：</p><ul><li><strong>数据逐步从源主节点复制到 7006</strong></li><li>客户端通过 <code>ASK</code> 重定向临时访问迁移中的 key</li><li><strong>服务不中断！</strong></li></ul></blockquote><h4 id="插槽数据迁移的具体实现" tabindex="-1"><a class="header-anchor" href="#插槽数据迁移的具体实现"><span>插槽数据迁移的具体实现</span></a></h4><p>那我们会想，假如按照<strong>最新的拓扑</strong>来，那么<strong>hash插槽1000会被从A挪到B</strong>，客户端直接拿到<strong>节点B</strong>，B怎么可能会知道<strong>hash插槽1000所对应的key</strong>跑哪去了呢？</p><p>而且我们只是把<strong>hash挪到了新的节点</strong>，我们又要怎么把其所<strong>对应的key都挪到新的节点</strong>呢？</p><p>事实上客户端还是会先走A，因为客户端的<strong>插槽节点映射数组</strong>压根没更新！（集群的所谓<strong>插槽节点映射关系</strong>也没有更新，只有彻底完成了迁移才会更新）</p><blockquote><p>客户端的<strong>插槽节点映射数组</strong>更新时机：</p><table><thead><tr><th style="text-align:left;">更新方式</th><th style="text-align:left;">触发条件</th><th style="text-align:left;">行为</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>被动更新（Reactive）</strong></td><td style="text-align:left;">收到 <code>MOVED</code> 或 <code>ASK</code> 错误</td><td style="text-align:left;">立即更新对应 slot 的节点映射</td></tr><tr><td style="text-align:left;"><strong>主动刷新（Proactive）</strong></td><td style="text-align:left;">定时任务（如每 30 秒）</td><td style="text-align:left;">重新执行 <code>CLUSTER SLOTS</code>，全量更新映射</td></tr></tbody></table><p>所以也不必担心多客户端情况，都会和集群同步的（但是定时任务吃资源）</p></blockquote><p><strong>🔑 核心机制：集群拓扑更新 ≠ 立即切换所有权</strong></p><p>Redis Cluster 的槽迁移是 <strong>“两阶段提交”式设计</strong>，通过 <strong><code>MIGRATING</code> / <code>IMPORTING</code> 状态 + 重定向</strong> 保证一致性。</p><hr><p><strong>✅ 详细流程（以 GET key 为例）</strong></p><p><strong>📌 前提：</strong></p><ul><li>槽 1000 正在从 <strong>A → B</strong> 迁移</li><li>集群拓扑（<code>CLUSTER SLOTS</code>）<strong>仍显示 slot 1000 属于 A</strong> （⚠️ 这是关键！拓扑不会提前切换）</li></ul><p><strong>步骤 1️⃣：客户端查拓扑 → 连 A</strong></p><ul><li>客户端缓存的拓扑中，<strong>slot 1000 仍指向 Node A</strong></li><li>所以它向 <strong>Node A</strong> 发送 <code>GET key</code></li></ul><p><strong>步骤 2️⃣：Node A 发现槽正在迁出</strong></p><ul><li>Node A 检查本地：key 存在 ✅</li><li>但槽 1000 处于 <code>MIGRATING</code> 状态</li><li><strong>Node A 返回：<code>ASK 1000 &lt;Node_B_IP:Port&gt;</code></strong></li></ul><blockquote><p>💡 <code>ASK</code> 是临时重定向（与 <code>MOVED</code> 不同）：</p><ul><li><code>MOVED</code> = 永久迁移完成</li><li><code>ASK</code> = “这个 key 可能要搬去 B，你先去 B 问问”</li></ul></blockquote><p><strong>步骤 3️⃣：客户端重试到 Node B</strong></p><ul><li><p>客户端收到 <code>ASK</code> 后：</p><ol><li>向 <strong>Node B</strong> 发送 <code>ASKING</code> 命令（特殊标记）</li><li>紧接着发送 <code>GET key</code></li></ol></li></ul><p><strong>步骤 4️⃣：Node B 处理</strong> <code>ASKING + GET</code></p><ul><li><p>Node B 收到 <code>ASKING</code>，知道这是迁移中的请求</p></li><li><p>Node B 向 Node A 发起 <code>MIGRATE</code> 命令：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">MIGRATE Node_A_IP Node_A_Port <span class="token string">&quot;&quot;</span> <span class="token number">0</span> <span class="token number">5000</span> KEYS key</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><p>Node A 将 <code>key</code> 的值发给 Node B，并删除本地 key</p></li><li><p>Node B 返回 <code>key</code> 的值给客户端</p></li></ul><p><strong>步骤 5️⃣：后续请求直连 Node B</strong></p><ul><li>槽 1000 <strong>迁移完成后</strong>（所有key均已完成迁移，手工确定），集群拓扑更新为 <strong>归属 Node B</strong></li><li>客户端刷新拓扑（被动或主动）</li><li>下次访问直接连 Node B，返回正常结果</li></ul><hr><p><strong>✅ 关键澄清：拓扑何时更新？</strong></p><table><thead><tr><th style="text-align:left;">阶段</th><th style="text-align:left;">拓扑中 slot 1000 归属</th><th style="text-align:left;">客户端行为</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>迁移前</strong></td><td style="text-align:left;">Node A</td><td style="text-align:left;">直连 A</td></tr><tr><td style="text-align:left;"><strong>迁移中</strong></td><td style="text-align:left;"><strong>仍为 Node A</strong></td><td style="text-align:left;">直连 A → 收到 <code>ASK</code> → 重试 B</td></tr><tr><td style="text-align:left;"><strong>迁移后</strong></td><td style="text-align:left;">Node B</td><td style="text-align:left;">直连 B</td></tr></tbody></table><blockquote><p>🚫 <strong>绝不会出现“拓扑已切到 B，但 key 还在 A”的情况！</strong> 因为 <strong>拓扑切换（<code>SETSLOT ... NODE</code>）是迁移完成的最后一步</strong>。</p></blockquote><p>而且<strong>迁移的触发</strong>不是交由集群自己去一个个遍历每个节点的key，而是由<strong>客户端访问该插槽</strong>，走<strong>插槽节点映射数组</strong> —&gt;拿到当前key所在节点（key在该节点或至少有迁移节点，迁移完成才会更新拓扑即<strong>插槽节点映射数组</strong>） —&gt;连接该节点看该hash槽位（不是key，<strong>节点不记录key和hash的映射</strong>）是否需要迁移，若需要则客户端再发一个ask到对应的新节点 —&gt;新节点接收到ask，根据信息向原节点请求获取对应key，原节点给出并删除该kv键值对，留一个去向元数据 —&gt;所有key完成迁移，slot完成迁移，这时手工确认迁移已经完成，集群更新拓扑，所有客户端被动/主动更新<strong>插槽节点映射数组</strong></p><p><strong>那假如一个slot有多个对应的key怎么办？</strong></p><table><thead><tr><th style="text-align:left;">问题</th><th style="text-align:left;">答案</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>Redis 会自动迁移槽内所有 key 吗？</strong></td><td style="text-align:left;">❌ <strong>不会！</strong> 默认只迁移被访问的 key</td></tr><tr><td style="text-align:left;"><strong>如何确保全量迁移？</strong></td><td style="text-align:left;">✅ 必须手动触发： <code>--cluster rebalance</code> 或自定义脚本</td></tr><tr><td style="text-align:left;"><strong>不迁移冷数据会怎样？</strong></td><td style="text-align:left;">⚠️ 执行 <code>SETSLOT ... NODE</code> 后，冷数据<strong>永久丢失</strong></td></tr><tr><td style="text-align:left;"><strong>迁移中断能恢复吗？</strong></td><td style="text-align:left;">✅ 能！槽状态持久化，支持续传</td></tr></tbody></table><h4 id="故障转移" tabindex="-1"><a class="header-anchor" href="#故障转移"><span>故障转移</span></a></h4><p>当主节点<strong>意外宕机</strong>时，从节点会自动发起故障转移。</p><p><strong>🔧 触发条件</strong></p><ol><li><p>主节点失联</p><ul><li>从节点检测到主节点 PING 超时（<code>cluster-node-timeout</code> 默认 15 秒）</li><li>并且<strong>大多数主节点也认为该主节点下线</strong>（避免脑裂）</li></ul></li><li><p>从节点满足晋升条件</p><ul><li>数据较新（复制偏移量接近主节点）</li></ul></li></ol><ul><li>优先级最高（多个从节点时）</li></ul><hr><p><strong>🔄 自动故障迁移流程</strong></p><p><strong>步骤 1️⃣：从节点标记主节点为</strong> <em><strong>*PFAIL（主观下线）*</strong></em></p><ul><li>某个从节点（如 A1）发现主节点 A 无响应</li><li>在本地将 A 标记为 <code>PFAIL</code></li></ul><p><strong>步骤 2️⃣：广播 PFAIL，达成</strong> <em><strong>*FAIL（客观下线）*</strong></em></p><ul><li>A1 向其他节点发送 <code>PING</code> 包，附带 “A is PFAIL”</li><li>当<strong>超过半数主节点</strong>同意 A 下线 → 集群将 A 标记为 <code>FAIL</code></li><li>所有节点广播 <code>FAIL</code> 消息</li></ul><blockquote><p>📌 <strong>为什么需要多数派？</strong> 防止网络分区时多个从节点同时晋升（脑裂）</p></blockquote><p><strong>步骤 3️⃣：从节点发起</strong> <em><strong>*FAILOVER_AUTH_REQUEST*</strong></em></p><ul><li>A1 向所有主节点发送选举请求</li><li>主节点回复 <code>FAILOVER_AUTH_ACK</code>（一票一票投）</li></ul><p><strong>步骤 4️⃣：获得多数票 → 晋升为主节点</strong></p><ul><li>A1 收到 ≥ <code>(N/2 + 1)</code> 票（N = 主节点总数）</li><li>A1 执行： <ol><li>将自己角色改为 <code>master</code></li><li>接管原主节点 A 的<strong>所有哈希槽</strong></li><li>广播 <code>PONG</code> 消息，宣告新身份</li></ol></li></ul><p><strong>步骤 5️⃣：客户端自动重连</strong></p><ul><li>客户端收到 <code>MOVED</code> 重定向（指向 A1）</li><li>更新本地拓扑缓存</li><li>后续请求直连新主节点 A1</li></ul><blockquote><p>⚡ 整个过程通常 <strong>10～30 秒</strong>（取决于 <code>cluster-node-timeout</code>）</p></blockquote><blockquote><p>还可能是运维<strong>自己关闭的主节点</strong>：</p><p>用于<strong>计划内维护</strong>（如升级主节点），<strong>不依赖主节点宕机</strong>。</p><p><strong>命令：</strong></p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 在从节点上执行</span></span>
<span class="line">redis-cli <span class="token parameter variable">-p</span> <span class="token number">7001</span> CLUSTER FAILOVER</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>🔄 流程特点：</strong></p><ol><li><strong>主节点必须在线</strong></li><li>从节点先与主节点<strong>同步最新数据</strong></li><li>然后安全切换，<strong>零数据丢失</strong></li><li>切换完成后，原主节点变为从节点</li></ol><p>✅ 适合：滚动升级、硬件更换等场景</p></blockquote><p>和哨兵的区别：</p><table><thead><tr><th style="text-align:left;">方面</th><th style="text-align:left;">Sentinel</th><th style="text-align:left;">Cluster</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>决策者</strong></td><td style="text-align:left;">Sentinel 进程（外部协调）</td><td style="text-align:left;"><strong>从节点</strong> + 多数主节点（去中心化）</td></tr><tr><td style="text-align:left;"><strong>选举方式</strong></td><td style="text-align:left;">Sentinel 投票</td><td style="text-align:left;">Raft-like 投票（主节点参与）</td></tr><tr><td style="text-align:left;"><strong>切换速度</strong></td><td style="text-align:left;">通常更快（1～10 秒）</td><td style="text-align:left;">稍慢（10～30 秒，默认 <code>node-timeout=15s</code>）</td></tr><tr><td style="text-align:left;"><strong>脑裂防护</strong></td><td style="text-align:left;">依赖 Sentinel 多数派</td><td style="text-align:left;">依赖主节点多数派</td></tr></tbody></table>`,276)])])}const r=n(l,[["render",i]]),d=JSON.parse('{"path":"/docs/heimadianping/gaojipian/1.fenbushihuancun.html","title":"分布式缓存","lang":"en-US","frontmatter":{"title":"分布式缓存","date":"2026-1-25"},"headers":[{"level":3,"title":"Redis持久化","slug":"redis持久化","link":"#redis持久化","children":[]},{"level":3,"title":"主从集群","slug":"主从集群","link":"#主从集群","children":[]},{"level":3,"title":"主从节点的数据同步","slug":"主从节点的数据同步","link":"#主从节点的数据同步","children":[]},{"level":3,"title":"哨兵机制","slug":"哨兵机制","link":"#哨兵机制","children":[]},{"level":3,"title":"分片集群","slug":"分片集群","link":"#分片集群","children":[]}],"git":{"createdTime":1771565155000,"updatedTime":1771565155000,"contributors":[{"name":"SaltFishGC","email":"130335482+SaltFishGC@users.noreply.github.com","commits":1}]},"filePathRelative":"docs/黑马点评/高级篇/1.分布式缓存.md"}');export{r as comp,d as data};
