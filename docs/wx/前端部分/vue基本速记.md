---
title: vue基本速记
date: 2025-11-28
---

### 基础



### debugger

 

### vue-router



### Component组件

#### 接收参数

#### scoped，lang=" "



### 回调函数

#### emit



### 全局挂载



### ref，reactive



### ：与@



### watch监听



### 字符串插值用`



### Promise

#### resolve（）

调用时注意异步，有时需要即时信息（组件不会自行刷新状态），如果没有做阻塞处理，那么就会返回一个promise而非正常数据（等待获取到数据才会添加result）

### 四种for循环

| 特性           | `for` (传统)            | `for...in`     | `for...of`      | `forEach`                |
| -------------- | ----------------------- | -------------- | --------------- | ------------------------ |
| **适用对象**   | 数组、类数组            | 对象（属性）   | 可迭代对象      | 数组、类数组             |
| **遍历内容**   | 索引                    | 属性名（键）   | 元素值          | 元素值 + 索引            |
| **是否可中断** | ✅ (`break`)             | ✅ (`break`)    | ✅ (`break`)     | ❌（`return` 仅跳过当前） |
| **是否可跳过** | ✅ (`continue`)          | ✅ (`continue`) | ✅ (`continue`)  | ❌（`return` 仅跳过当前） |
| **获取索引**   | 直接 (`i`)              | 需 `key`       | 需 `.entries()` | 回调参数 `index`         |
| **获取值**     | `arr[i]`                | `obj[key]`     | 直接            | 回调参数 `item`          |
| **遍历原型链** | 否                      | 是（需注意）   | 否              | 否                       |
| **遍历顺序**   | 明确                    | 不保证         | 插入顺序        | 按索引顺序               |
| **性能**       | ⭐⭐⭐⭐⭐ (最快)            | ⭐⭐             | ⭐⭐⭐⭐            | ⭐⭐⭐                      |
| **函数式风格** | 否                      | 否             | 否              | ✅                        |
| **异步支持**   | ✅（配合 `async/await`） | ✅              | ✅               | ❌（不等待异步）          |
| **ES 版本**    | ES3                     | ES5            | ES6             | ES5                      |

| 场景                              | 推荐方式                                                     |
| --------------------------------- | ------------------------------------------------------------ |
| 遍历数组，需要高性能或精确控制    | ✅ 传统 `for`                                                 |
| 遍历数组，代码简洁，无需中断      | ✅ `forEach`                                                  |
| 遍历数组，需要 `break`/`continue` | ✅ `for...of`                                                 |
| 遍历字符串、Set、Map 等可迭代对象 | ✅ `for...of`                                                 |
| 遍历普通对象的自身可枚举属性      | ✅ `for...in` + `hasOwnProperty` 或 `Object.keys()` + `for...of` |
| 函数式编程风格，链式调用          | ✅ `forEach`（或 `map`、`filter` 等）                         |



### pinia

贼好用的工具库[简介 | Pinia](https://pinia.vuejs.org/zh/introduction.html)

#### watch监听状态变化

### async异步 await阻塞同步



### scss样式



### node的文件处理



### 页面/组件跳转

#### router编制索引

#### 跳转（内部组件跳转）



### 样式

#### 元素定位

[开发者工具（F12）进行元素定位 - 迷糊的我 - 博客园](https://www.cnblogs.com/zaigua/p/18420720#:~:text=在 Elements 面板左上角，有一个小箭头图标（有时叫“选择元素”工具），点击它后，可以在页面上悬停选择具体的网页元素。 当鼠标悬停在网页元素上时，该元素会被高亮显示，并且在开发者工具中自动展开对应的 HTML,代码。 选择工具启用后，点击你感兴趣的页面部分。 开发者工具会自动定位到该元素的 HTML 代码，并显示相关的属性、类名、ID 等。)

开发者工具左上角有个箭头，选择后点击组件即可追踪其所在html位置
